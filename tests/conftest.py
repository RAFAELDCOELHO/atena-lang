"""
Shared pytest fixtures for the Atena test suite.

This file is populated by each phase plan as new fixtures are needed.
Currently empty — the scaffold is here so pytest discovers this package
and future plans can add fixtures without modifying the test-collection
setup.
"""
