---
phase: "05-cli-runtime-pipeline-integration"
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/atena/pipeline.py
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - CLI-03

must_haves:
  truths:
    - "transpile(source, filename) returns a Python string when there are no errors"
    - "transpile(source, filename) returns None when any phase produces errors"
    - "Errors from each phase are printed to stderr in the canonical format before returning None"
    - "Codegen never runs unless errors.is_empty() is True (GEN-03 contract enforced)"
    - "A valid show 1 program transpiles to a Python string containing print(1)"
    - "A program with a lexer-level error (e.g. unterminated string) causes transpile to return None"
    - "A program with a semantic-level error causes transpile to return None without running codegen"
  artifacts:
    - path: "src/atena/pipeline.py"
      provides: "transpile(source, filename) -> str | None — four-phase wiring with between-phase gating"
      exports: ["transpile"]
      contains: "def transpile"
  key_links:
    - from: "src/atena/pipeline.py"
      to: "src/atena/lexer.py"
      via: "Lexer(source, errors).tokenize()"
      pattern: "Lexer\\(source"
    - from: "src/atena/pipeline.py"
      to: "src/atena/codegen.py"
      via: "CodeGenerator(program).generate() — called only when errors.is_empty()"
      pattern: "errors\\.is_empty"
---

<objective>
Implement the transpile() driver in src/atena/pipeline.py — the four-phase pipeline that wires Lexer → Parser → Analyzer → Generator with between-phase error gating.

Purpose: Every other Phase 5 plan depends on this function. The CLI verbs run/build both call transpile() to get a Python string (or None on error). Without this, the NotImplementedError stub prevents any end-to-end test from passing.

Output: src/atena/pipeline.py with a working transpile(source, filename) -> str | None that satisfies CLI-01, CLI-02, and CLI-03 at the pipeline level.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-cli-runtime-pipeline-integration/05-CONTEXT.md
@.planning/phases/05-cli-runtime-pipeline-integration/05-PATTERNS.md

<interfaces>
<!-- Key contracts the executor must call — do not modify these, only call them. -->

From src/atena/errors.py:
  class ErrorCollector:
      def add(self, line: int, message: str, source_line: str) -> None
      def is_empty(self) -> bool
      def report(self) -> str  # returns canonical formatted block; "" if no errors

From src/atena/lexer.py:
  class Lexer:
      def __init__(self, source: str, errors: ErrorCollector) -> None
      def tokenize(self) -> list[Token]

From src/atena/parser.py:
  class Parser:
      def __init__(self, tokens: list[Token], errors: ErrorCollector) -> None
      def parse(self) -> Program

From src/atena/analyzer.py:
  class SemanticAnalyzer:
      def __init__(self, program: Program, errors: ErrorCollector) -> None
      def analyze() -> Program

From src/atena/codegen.py:
  class CodeGenerator:
      def __init__(self, program: Program) -> None  # no errors arg — called only when errors.is_empty()
      def generate(self) -> str

Current pipeline.py stub (entire file — replace this):
  def transpile(source: str, filename: str) -> str | None:
      raise NotImplementedError("Pipeline not built yet — Phase 5")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement transpile() with four-phase wiring and between-phase gating</name>
  <files>src/atena/pipeline.py</files>
  <read_first>
    - src/atena/pipeline.py (current stub — replace the body)
    - src/atena/errors.py (ErrorCollector.is_empty(), report() format — lines 39-85)
    - .planning/phases/05-cli-runtime-pipeline-integration/05-PATTERNS.md (pipeline shape pattern — lines 60-83, error report format lines 86-99)
    - src/atena/codegen.py (lines 1-30 — confirms CodeGenerator takes only program, no errors param)
  </read_first>
  <behavior>
    - transpile("show 1\n", "test.atena") returns a str containing "print" (not None)
    - transpile('show "hello"\n', "test.atena") returns a str containing "print"
    - transpile('x = "\n', "test.atena") returns None (unterminated string — lexer error)
    - transpile("xyz + 1\n", "test.atena") returns None if xyz undefined (analyzer error)
    - On any error in any phase, errors.report() has been printed to stderr before returning None
    - CodeGenerator is never instantiated when errors list is non-empty
    - A program with only a parser-level syntax error returns None without reaching the analyzer
    - A program with only an analyzer-level error returns None without reaching codegen
  </behavior>
  <action>
    Replace the NotImplementedError stub body in src/atena/pipeline.py with a real implementation that:

    1. Imports: from __future__ import annotations; import sys; from atena.errors import ErrorCollector; from atena.lexer import Lexer; from atena.parser import Parser; from atena.analyzer import SemanticAnalyzer; from atena.codegen import CodeGenerator.

    2. Defines transpile(source: str, filename: str) -> str | None.

    3. Instantiates a single shared ErrorCollector() — injected into each phase.

    4. Runs phases in sequence with between-phase gating (never within a phase):
       a. tokens = Lexer(source, errors).tokenize()
       b. if not errors.is_empty(): print(errors.report(), file=sys.stderr); return None
       c. program = Parser(tokens, errors).parse()
       d. if not errors.is_empty(): print(errors.report(), file=sys.stderr); return None
       e. program = SemanticAnalyzer(program, errors).analyze()
       f. if not errors.is_empty(): print(errors.report(), file=sys.stderr); return None
       g. return CodeGenerator(program).generate()   # GEN-03: only when errors.is_empty()

    Keep the module docstring updated to reflect the real implementation.
    No external dependencies — stdlib only (sys already available).
    The filename parameter is reserved for future use (e.g. compile() target in cli.py) — accept it but the current pipeline does not need to pass it anywhere.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -c "from atena.pipeline import transpile; r = transpile('show 1\n', 'test.atena'); assert r is not None and 'print' in r, repr(r); print('transpile OK:', repr(r[:40]))"</automated>
  </verify>
  <done>
    - src/atena/pipeline.py no longer raises NotImplementedError
    - transpile("show 1\n", "test.atena") returns a non-None str containing "print"
    - transpile with a syntax error returns None and prints to stderr
    - transpile with a semantic error returns None without running codegen
    - The function signature is def transpile(source: str, filename: str) -> str | None
  </done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test the full pipeline with school.atena</name>
  <files>tests/test_pipeline.py</files>
  <read_first>
    - src/atena/pipeline.py (just implemented in Task 1)
    - examples/school.atena (the golden fixture — already complete, no modifications)
    - .planning/phases/05-cli-runtime-pipeline-integration/05-PATTERNS.md (lines 331-343 stdin passthrough pattern)
    - tests/test_cli.py (lines 28-34 run_cli helper, lines 37-43 fixture pattern)
  </read_first>
  <behavior>
    - transpile(school_source, "school.atena") returns a non-None str
    - The returned string contains "def make_greeting" (function was emitted)
    - The returned string contains "input(" (ask statement was emitted)
    - transpile("show 1\n", "t.atena") returns a string with "print(1)"
    - transpile("x = \"\n", "t.atena") returns None (lexer error)
    - transpile("if\n", "t.atena") returns None (parser error)
    - transpile("show xyz\n", "t.atena") returns None (analyzer error — undefined xyz)
  </behavior>
  <action>
    Create tests/test_pipeline.py with parametrized and fixture-based tests for transpile():

    1. Import: from atena.pipeline import transpile; import os; from pathlib import Path.

    2. test_transpile_show_number: transpile("show 1\n", "t.atena") returns str containing "print".

    3. test_transpile_lexer_error_returns_none: transpile('show "\n', "t.atena") returns None (unterminated string forces lexer error).

    4. test_transpile_parser_error_returns_none: transpile("if\n", "t.atena") returns None.

    5. test_transpile_analyzer_error_returns_none: transpile("show xyz\n", "t.atena") returns None (xyz undefined).

    6. test_transpile_school_atena: Read examples/school.atena via Path("examples/school.atena").read_text(), call transpile(source, "school.atena"), assert result is not None, assert "def make_greeting" in result, assert "input(" in result.

    7. test_transpile_error_printed_to_stderr (capsys): call transpile('show "\n', "t.atena"), capture with capsys, assert "Error on line" in capsys.readouterr().err.

    Use pytest fixtures where appropriate. All tests use the parametrize or direct call style — no subprocess in this file (that belongs in test_cli.py).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -m pytest tests/test_pipeline.py -v --no-header 2>&1 | tail -20</automated>
  </verify>
  <done>
    - tests/test_pipeline.py exists with at least 6 tests
    - All tests in tests/test_pipeline.py pass (exit 0)
    - school.atena transpiles to a non-None result with make_greeting and input() present
    - Error paths return None and print to stderr in canonical format
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Atena source text → pipeline | Untrusted text enters at transpile(source); phases parse and analyze it |
| Analyzed AST → CodeGenerator | Trusted — CodeGenerator runs only after errors.is_empty() confirms a clean tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-A | Tampering | pipeline.py — codegen gating | mitigate | errors.is_empty() gate before CodeGenerator() is enforced structurally; codegen never receives a partial/error AST |
| T-05-01-B | Denial of Service | pipeline.py — between-phase gating | accept | single-file programs from learners; no loop/recursion in the driver; cost is proportional to source length |
| T-05-01-C | Information Disclosure | pipeline.py stderr output | mitigate | errors.report() emits only the canonical plain-English format — no Python stack frames, no internal type names |
| T-05-01-SC | Tampering | No new package installs in this plan | accept | stdlib only (sys); no npm/pip install steps; slopcheck N/A |
</threat_model>

<verification>
Full suite stays green:

```bash
cd /Users/juliorcoelho/atena-lang && python -m pytest tests/ -q --no-header 2>&1 | tail -5
```

Pipeline smoke:
```bash
cd /Users/juliorcoelho/atena-lang && python -c "
from atena.pipeline import transpile
result = transpile('show 1\n', 'test.atena')
assert result is not None
assert 'print' in result
print('pipeline OK')
"
```
</verification>

<success_criteria>
- src/atena/pipeline.py implements transpile(source, filename) -> str | None with real four-phase wiring
- Between-phase gating: any error stops the pipeline and prints to stderr before returning None
- GEN-03: CodeGenerator is only instantiated when errors.is_empty() is True
- tests/test_pipeline.py exists; all tests pass
- Full 253-test suite + new pipeline tests pass (exit 0)
- school.atena transpiles to a non-None Python string
</success_criteria>

<output>
Create .planning/phases/05-cli-runtime-pipeline-integration/05-01-SUMMARY.md when done.
</output>
