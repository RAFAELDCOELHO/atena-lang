---
phase: 01-lexer
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_lexer.py
  - src/atena/lexer.py
autonomous: true
requirements:
  - LEX-01
  - LEX-02
  - LEX-03
  - LEX-04
  - LEX-05
  - LEX-06
  - LEX-07
  - LEX-08

must_haves:
  truths:
    - "tests/test_lexer.py exists with all 28 test functions named exactly as in RESEARCH.md"
    - "Every test in test_lexer.py fails with ImportError or AttributeError (RED phase — lexer.py is a stub)"
    - "The full Phase 0 test suite still passes (58 tests) — no regression"
    - "src/atena/lexer.py exists with class Lexer and a tokenize() stub that raises NotImplementedError"
  artifacts:
    - path: "tests/test_lexer.py"
      provides: "All 28 RED-phase tests covering LEX-01 through LEX-08"
      min_lines: 120
    - path: "src/atena/lexer.py"
      provides: "Lexer stub — class Lexer with __init__(source, errors) and tokenize() -> list[Token]"
      contains: "class Lexer"
  key_links:
    - from: "tests/test_lexer.py"
      to: "src/atena/lexer.py"
      via: "from atena.lexer import Lexer"
      pattern: "from atena\\.lexer import Lexer"
    - from: "tests/test_lexer.py"
      to: "src/atena/tokens.py"
      via: "from atena.tokens import TokenType, Token, KEYWORDS"
      pattern: "from atena\\.tokens import"
---

<objective>
Write all 28 failing tests (RED phase per CLAUDE.md TDD requirement), then create a minimal stub
implementation so imports succeed and tests fail with assertion/NotImplementedError rather than
ImportError.

Purpose: Establish the complete test contract for the Atena Lexer before any implementation
exists. Every test in this plan must FAIL after this plan completes — that is the definition
of done for a TDD RED phase.

Output: tests/test_lexer.py (28 test functions, all failing) + src/atena/lexer.py (stub).
</objective>

<execution_context>
@/Users/juliorcoelho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliorcoelho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-lexer/01-CONTEXT.md
@.planning/phases/01-lexer/01-RESEARCH.md
@.planning/phases/01-lexer/01-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write all RED-phase tests in tests/test_lexer.py</name>
  <files>tests/test_lexer.py</files>

  <read_first>
    - tests/test_errors.py (error-path assertion style: key phrase in report, "Error on line N" check)
    - tests/test_tokens.py (naming convention test_T{N}_*, plain assert, no test classes)
    - tests/conftest.py (currently empty; no existing fixtures)
    - src/atena/tokens.py (TokenType members, Token fields: type/value/line/col/source_line, KEYWORDS dict with 19 entries)
    - src/atena/errors.py (ErrorCollector.add(line, message, source_line), is_empty(), report())
    - .planning/phases/01-lexer/01-RESEARCH.md §"Phase Requirements → Test Map" (all 28 test function names verbatim)
    - .planning/phases/01-lexer/01-CONTEXT.md §decisions (D-01 through D-08, off-ramp messages)
  </read_first>

  <behavior>
    File header:
    - Module docstring: "TDD tests for the Atena Lexer — Phase 1 / Wave 0 RED phase." plus the two-layer description.
    - Imports: `from __future__ import annotations`, then `import pytest`, then `from atena.tokens import TokenType, Token, KEYWORDS`, `from atena.errors import ErrorCollector`, `from atena.lexer import Lexer`.
    - Module-level helper (not a fixture): `def _lex(source: str) -> tuple[list[Token], ErrorCollector]` — constructs `ErrorCollector()`, runs `Lexer(source, ec).tokenize()`, returns `(tokens, ec)`.

    Naming convention: `test_L{req_id}_{description}` exactly as listed in RESEARCH.md §"Phase Requirements → Test Map". Use `test_Lx_` prefix for cross-requirement tests.

    Layer 1 — golden snapshot tests (assert exact token types/values, no errors):

    - test_L1_all_token_types: lex a hand-crafted source that exercises all 17 non-structural token types (STRING, NUMBER, IDENTIFIER, KEYWORD, OPERATOR, COMPARISON, ASSIGN, LPAREN, RPAREN, LBRACKET, RBRACKET, LBRACE, RBRACE, COMMA, DOT; NEWLINE/INDENT/DEDENT/EOF are tested elsewhere). Assert each token type appears at least once in the stream; assert `ec.is_empty()`.
    - test_L1_keyword_recognition: lex `"show x\n"`, assert the first token has `type == TokenType.KEYWORD` and `value == "show"`.
    - test_L1_identifier_vs_keyword: lex `"score = 10\n"`, assert `"score"` produces `TokenType.IDENTIFIER` and `"="` produces `TokenType.ASSIGN`.
    - test_L2_indent_dedent_balanced: lex `"if x\n    show y\n"` (4-space indent), assert the token stream contains exactly one INDENT and one DEDENT, and the stream ends with EOF.
    - test_L2_eof_drain_no_trailing_newline: lex `"if x\n    show y"` (no trailing newline), assert one INDENT and one DEDENT appear and the last token is EOF.
    - test_L2_eof_drain_mid_block: lex `"if x\n    if y\n        show z"` (no trailing newline, two open blocks), assert exactly two INDENT tokens and two DEDENT tokens, last token EOF.
    - test_L3_blank_line_no_tokens: lex `"show 1\n\nshow 2\n"`, assert no INDENT/DEDENT tokens appear and `ec.is_empty()`.
    - test_L3_comment_only_no_tokens: lex `"show 1\n# this is a comment\nshow 2\n"`, assert no INDENT/DEDENT tokens and `ec.is_empty()`.
    - test_L3_deep_comment_no_indent_effect: lex `"if x\n    show y\n        # deep comment\n    show z\n"`, assert the INDENT/DEDENT count is still 1 each (the deep comment does not open a new block) and `ec.is_empty()`.
    - test_L5_assign_token: lex `"x = 10\n"`, assert exactly one ASSIGN token with `value == "="` and `ec.is_empty()`.
    - test_L5_eq_comparison_token: lex `"x == 10\n"`, assert exactly one COMPARISON token with `value == "=="` and `ec.is_empty()`.
    - test_L5_all_comparisons: lex `"a != b\na < b\na > b\na <= b\na >= b\n"`, assert each of `!=`, `<`, `>`, `<=`, `>=` appears exactly once as COMPARISON. Assert `ec.is_empty()`.
    - test_L5_arithmetic_operators: lex `"a + b - c * d / e\n"`, assert OPERATOR tokens with values `+`, `-`, `*`, `/` each appear exactly once. Assert `ec.is_empty()`.
    - test_L6_all_19_keywords: lex a source line that contains all 19 keywords (show ask if else while repeat times and or not function return add to remove from length true false — each as a bare word, separated by spaces, on one line ending with `\n`). Assert exactly 19 KEYWORD tokens appear. Assert `ec.is_empty()`.
    - test_L7_string_literal: lex `'"hello"\n'`, assert one STRING token with `value == "hello"` and `ec.is_empty()`.
    - test_L7_number_literal: lex `"42\n"`, assert one NUMBER token with `value == "42"` and `ec.is_empty()`.
    - test_L7_token_position_fields: lex `"x = 10\n"`, assert every non-EOF token has `line >= 1` and `source_line == "x = 10\n"` (or `"x = 10"` — accept either with or without trailing newline as long as it is not empty). Assert `ec.is_empty()`.

    Layer 2 — error-path tests (assert key phrase in report(), error line number, no crash):

    - test_L4_mixed_tabs_spaces_error: source is `"if x\n    show y\n\tshow z\n"` (4-space indent on line 2, then tab on line 3). Assert `not ec.is_empty()`. Assert `"tabs and spaces"` in `ec.report()`.
    - test_L4_staircase_dedent_error: source is `"if x\n    show y\n  show z\n"` (dedent to column 2, which was never a stack level). Assert `not ec.is_empty()`. Assert `"doesn't match"` in `ec.report()`.
    - test_L4_over_indent_error: source is `"if x\n        show y\n"` (8-space jump from 0, more than one unit). When the indentation unit is being pinned (first indent), a jump from 0 to 8 spaces sets unit=8 (single unit), so this does NOT error. Adjust: use `"if x\n    show y\n        show z\n"` where unit is pinned at 4 but line 3 jumps by 8 (0→4→12 would be over-indent). Actually: source `"if x\n    if y\n            show z\n"` where second indent is 4 (unit=4) but third is 12 (jump of 8 = 2 units). Assert `not ec.is_empty()`. Assert `"too far"` in `ec.report()`.
    - test_L4_ragged_width_error: source uses 4-space unit on first indent, then indents by 6 (a non-multiple of 4). Example: `"if x\n    if y\n      show z\n"` (4-space unit, then 6-space second indent). Assert `not ec.is_empty()`. Assert `"same size"` in `ec.report()`.
    - test_L8_unterminated_string: lex `'"hello\n'` (opening `"` but no closing `"`). Assert `not ec.is_empty()`. Assert `"Error on line 1"` in `ec.report()`. Assert no Python exception escapes.
    - test_L8_decimal_offramp: lex `"x = 3.5\n"`. Assert `not ec.is_empty()`. Assert `"whole numbers"` in `ec.report()`. Assert `"Error on line 1"` in `ec.report()`.
    - test_L8_single_quote_offramp: lex `"x = 'hello'\n"`. Assert `not ec.is_empty()`. Assert `"double quotes"` in `ec.report()`. Assert `"Error on line 1"` in `ec.report()`.
    - test_L8_colon_offramp: lex `"if x > 1:\n"`. Assert `not ec.is_empty()`. Assert `"colons"` in `ec.report()` (key phrase from D-02 message: "Atena doesn't use colons"). Assert `"Error on line 1"` in `ec.report()`.
    - test_L8_semicolon_offramp: lex `"a = 1; b = 2\n"`. Assert `not ec.is_empty()`. Assert `"own line"` in `ec.report()` (key phrase: "Put each step on its own line"). Assert `"Error on line 1"` in `ec.report()`.
    - test_L8_unexpected_char: lex `"x = @foo\n"`. Assert `not ec.is_empty()`. Assert `"Error on line 1"` in `ec.report()`. Assert no Python exception escapes.
    - test_Lx_multiple_errors_collected: lex `'"hello\nx = 3.5\n'` (unterminated string on line 1, decimal on line 2). Assert `not ec.is_empty()`. Assert `"Error on line 1"` in `ec.report()`. Assert `"Error on line 2"` in `ec.report()` (both errors collected in one run).
    - test_Lx_offramp_no_infinite_loop: lex `"x = 'abc'; y = 3.5\n"` (single-quote and decimal together). Assert returns without hanging. Assert `not ec.is_empty()`. (Use pytest timeout or just assert returns — no loop guard needed if the always-make-progress invariant holds.)

    All tests must be written as top-level functions, no test classes. Plain `assert`. No `@pytest.mark.skip`.
  </behavior>

  <action>
    Create tests/test_lexer.py. Begin with the module docstring, `from __future__ import annotations`, then imports of pytest, TokenType/Token/KEYWORDS from atena.tokens, ErrorCollector from atena.errors, and Lexer from atena.lexer. Write the module-level `_lex()` helper. Then write all 28 test functions in the order listed in the behavior block above. Do NOT use fenced code blocks in this action — consult the behavior block for exact assertion patterns. Follow the `test_L{req_id}_{description}` naming convention. Every test body must have a docstring (one sentence) describing what it verifies. Use `assert` for all checks; use `in` for substring checks on report strings (key phrase, not full sentence). Do not import inside test functions; all imports are at module level.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_lexer.py -q --tb=no 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_lexer.py exists and is valid Python (python3 -c "import ast; ast.parse(open('tests/test_lexer.py').read())" exits 0).
    - The file contains exactly 28 functions matching the test_L*/test_Lx* naming pattern.
    - Running `python3 -m pytest tests/test_lexer.py --tb=no -q` shows ALL tests failing (expected at this stage — no lexer.py yet, so ImportError or attribute error).
    - No test function is decorated with @pytest.mark.skip.
  </acceptance_criteria>

  <done>All 28 test functions written; all fail with ImportError (src/atena/lexer.py does not exist yet).</done>
</task>

<task type="auto">
  <name>Task 2: Create src/atena/lexer.py stub so tests fail with assertion/NotImplementedError</name>
  <files>src/atena/lexer.py</files>

  <read_first>
    - src/atena/tokens.py (Token, TokenType, KEYWORDS — the output contract; lexer imports these)
    - src/atena/errors.py (ErrorCollector.add() — the only error channel; lexer imports this)
    - .planning/phases/01-lexer/01-PATTERNS.md §"Imports pattern" and §"Class constructor / dependency-injection pattern" (exact import block and Lexer.__init__ signature)
    - .planning/phases/01-lexer/01-RESEARCH.md §"Lexer Class Skeleton" (exact constructor field list)
  </read_first>

  <action>
    Create src/atena/lexer.py. Include:
    1. Module docstring describing the file (match the style from errors.py: three-sentence contract description mentioning that the lexer never raises to the user, only calls ErrorCollector.add, and returns list[Token]).
    2. `from __future__ import annotations` first.
    3. Imports: `from atena.tokens import TokenType, Token, KEYWORDS` and `from atena.errors import ErrorCollector`. No other imports.
    4. Class `Lexer` with constructor `__init__(self, source: str, errors: ErrorCollector) -> None` that sets: `self._source = source`, `self._errors = errors`, `self._lines = source.splitlines(keepends=True)`, `self._pos = 0`, `self._line = 1`, `self._col = 0`, `self._indent_stack: list[int] = [0]`, `self._indent_char: str | None = None`, `self._indent_unit: int | None = None`, `self._tokens: list[Token] = []`.
    5. Public method `tokenize(self) -> list[Token]` that raises `NotImplementedError("Lexer.tokenize() not yet implemented — RED phase")`.
    No other methods. This is the minimal stub for the RED phase.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_lexer.py --tb=line -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - src/atena/lexer.py exists and is importable: `python3 -c "from atena.lexer import Lexer"` exits 0.
    - `python3 -m pytest tests/test_lexer.py --tb=no -q` shows 28 FAILED (not ImportError — import succeeds but tokenize() raises NotImplementedError, so all tests that call _lex() fail with NotImplementedError or a downstream assertion failure).
    - `python3 -m pytest tests/ --tb=no -q` shows Phase 0 tests still passing (58 green) plus 28 new failures (lexer tests). Total: 58 passed, 28 failed, 0 errors. No Phase 0 regression.
    - `grep -v "^#" src/atena/lexer.py | grep -c "class Lexer"` returns 1.
  </acceptance_criteria>

  <done>
    stub committed on feat/lexer; `python3 -m pytest tests/ --tb=no -q` shows 28 failed (lexer RED) + 58 passed (Phase 0 green). No imports broken.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → lexer stub | Tests call Lexer(source, ec).tokenize(); source is trusted test input at this stage. No untrusted input in the stub. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-W0-01 | Tampering | tests/test_lexer.py (test correctness) | mitigate | Tests assert key phrases from D-02 messages, not exact strings — implementation has wording flexibility but substance is pinned. Any future rewording that drops the key phrase will fail a test. |
| T-01-W0-02 | Denial of Service | tokenize() stub (infinite-loop risk) | accept | Stub raises NotImplementedError immediately — no loop, no hang risk. Real scan-loop DOS mitigation is addressed in Plan 02/03. |
| T-01-W0-SC | Tampering | No npm/pip/cargo installs | accept | Zero new package installs in this plan. stdlib + sibling modules only. |
</threat_model>

<verification>
After both tasks complete:
- `python3 -c "from atena.lexer import Lexer; print('import ok')"` prints "import ok".
- `python3 -m pytest tests/test_lexer.py --tb=no -q` shows 28 failed, 0 passed, 0 errors.
- `python3 -m pytest tests/ --tb=no -q` shows 28 failed, 58 passed, 0 errors.
- `grep -c "def test_L" tests/test_lexer.py` returns 28.
</verification>

<success_criteria>
TDD RED phase complete: 28 lexer tests exist and fail, 58 Phase 0 tests remain green. The test file locks in the complete behavioral contract for the lexer. No implementation in src/atena/lexer.py beyond the minimal stub. Wave 0 is complete — Plans 02 and 03 can proceed.
</success_criteria>

<output>
Create .planning/phases/01-lexer/01-01-SUMMARY.md when done.
</output>
