---
phase: "00"
plan: "03"
type: tdd
wave: 2
depends_on:
  - "00-01"
files_modified:
  - src/atena/tokens.py
  - src/atena/ast_nodes.py
  - tests/test_tokens.py
  - tests/test_ast_nodes.py
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - DIAG-05
must_haves:
  truths:
    - "Token carries type, value, line, col, and source_line so any phase can produce 'Error on line {N}: … → {source}' without re-reading the file (DIAG-01)"
    - "Every AST node carries line (and source_line) so the analyzer can produce located errors at any tree node (DIAG-01)"
    - "tokens.py and ast_nodes.py import with ZERO non-stdlib, non-builtin dependencies (ARCHITECTURE.md pure-data contract)"
    - "Token and every AST node have a usable __eq__ and __repr__ (via @dataclass) so parser/analyzer tests can assert node equality without writing __eq__ by hand"
    - "The full node-set from ARCHITECTURE.md Pattern 8 is defined: all 22 node types present and importable"
    - "TokenType is an IntEnum (or Enum) covering all 19 token types from REQUIREMENTS.md LEX-01"
  artifacts:
    - path: "src/atena/tokens.py"
      provides: "TokenType enum (19 members), Token dataclass (5 fields), KEYWORDS dict mapping Atena keyword strings to their TokenType"
      exports: ["TokenType", "Token", "KEYWORDS"]
    - path: "src/atena/ast_nodes.py"
      provides: "Node base dataclass + 22 concrete AST node dataclasses, all position-bearing"
      exports: ["Node", "Program", "Assign", "Show", "Ask", "If", "While", "Repeat", "FunctionDef", "Return", "FunctionCall", "BinOp", "UnaryOp", "ListLiteral", "DictLiteral", "IndexAccess", "DotAccess", "ListAdd", "ListRemove", "Identifier", "NumberLiteral", "StringLiteral", "BoolLiteral"]
    - path: "tests/test_tokens.py"
      provides: "Tests: TokenType values, Token construction, KEYWORDS entries, dataclass __eq__"
    - path: "tests/test_ast_nodes.py"
      provides: "Tests: all 22 node types constructable and comparable via __eq__"
  key_links:
    - from: "tests/test_tokens.py"
      to: "src/atena/tokens.py"
      via: "from atena.tokens import TokenType, Token, KEYWORDS"
      pattern: "from atena\\.tokens import"
    - from: "tests/test_ast_nodes.py"
      to: "src/atena/ast_nodes.py"
      via: "from atena.ast_nodes import Program, Assign, BinOp, ..."
      pattern: "from atena\\.ast_nodes import"
---

<objective>
Implement the two pure-data inter-phase contract modules: `tokens.py` (TokenType enum, Token dataclass, KEYWORDS map) and `ast_nodes.py` (22 position-bearing AST node dataclasses). These modules have zero sibling-module dependencies and are the contracts every later phase imports.

Purpose: Lexer, Parser, Analyzer, and Generator all depend on these types. Defining them here, with tests, prevents type drift and lets each phase test construct expected tokens/nodes directly.
Output: Fully-tested `tokens.py` and `ast_nodes.py`; green `tests/test_tokens.py` and `tests/test_ast_nodes.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/juliorcoelho/atena-lang/.planning/PROJECT.md
@/Users/juliorcoelho/atena-lang/.planning/ROADMAP.md
@/Users/juliorcoelho/atena-lang/.planning/phases/00-diagnostics-spine-data-contracts/00-CONTEXT.md

<interfaces>
<!-- tokens.py contract (from ARCHITECTURE.md + REQUIREMENTS.md LEX-01 + LEX-06): -->
<!--                                                                                -->
<!-- TokenType members (19 types from REQUIREMENTS.md LEX-01):                     -->
<!--   STRING, NUMBER, IDENTIFIER, OPERATOR, COMPARISON, ASSIGN,                  -->
<!--   LPAREN, RPAREN, LBRACKET, RBRACKET, LBRACE, RBRACE,                        -->
<!--   COMMA, DOT, NEWLINE, INDENT, DEDENT, EOF, KEYWORD                          -->
<!--   (KEYWORD used for Atena keywords; IDENTIFIER for user-defined names)        -->
<!--                                                                                -->
<!-- KEYWORDS dict — maps string literals to TokenType.KEYWORD:                    -->
<!--   {"show": TokenType.KEYWORD, "ask": TokenType.KEYWORD, "if": ..., ...}       -->
<!--   Full set (LEX-06): show ask if else while repeat times and or not            -->
<!--                       function return add to remove from length true false     -->
<!--                                                                                -->
<!-- Token dataclass (frozen=True — tokens are immutable after creation):          -->
<!--   type: TokenType                                                              -->
<!--   value: str                                                                   -->
<!--   line: int                                                                    -->
<!--   col: int                                                                     -->
<!--   source_line: str                                                             -->
<!--                                                                                -->
<!-- ast_nodes.py contract (from ARCHITECTURE.md Pattern 8):                       -->
<!-- Node base: @dataclass with line: int, source_line: str = ""                   -->
<!-- 22 concrete nodes (each inherits Node, adds its fields):                      -->
<!--   Program(statements: list[Node])                                             -->
<!--   Assign(name: str, value: Node)          Show(value: Node)                  -->
<!--   Ask(prompt: str, target: str)           If(condition: Node,                 -->
<!--                                              then_body: list[Node],           -->
<!--                                              else_body: list[Node])           -->
<!--   While(condition: Node, body: list[Node])                                   -->
<!--   Repeat(count: Node, body: list[Node])                                       -->
<!--   FunctionDef(name: str, params: list[str], body: list[Node])                -->
<!--   Return(value: Node)                                                         -->
<!--   FunctionCall(name: str, args: list[Node])                                  -->
<!--   BinOp(op: str, left: Node, right: Node)                                    -->
<!--   UnaryOp(op: str, operand: Node)                                             -->
<!--   ListLiteral(elements: list[Node])                                           -->
<!--   DictLiteral(pairs: list[tuple[str, Node]])                                  -->
<!--   IndexAccess(target: Node, index: Node)   (index_converted: bool = False)   -->
<!--   DotAccess(target: Node, name: str)                                          -->
<!--   ListAdd(target: str, value: Node)                                           -->
<!--   ListRemove(target: str, value: Node)                                        -->
<!--   Identifier(name: str)                                                       -->
<!--   NumberLiteral(value: int)                                                   -->
<!--   StringLiteral(value: str)                                                   -->
<!--   BoolLiteral(value: bool)                                                    -->
<!--                                                                                -->
<!-- Note: IndexAccess has index_converted: bool = False (ARCHITECTURE.md          -->
<!-- Pattern 11 — analyzer sets True after rewrite; prevents double-shift).        -->
</interfaces>
</context>

<feature>
  <name>Token + AST node data contracts</name>
  <files>src/atena/tokens.py, src/atena/ast_nodes.py, tests/test_tokens.py, tests/test_ast_nodes.py</files>
  <behavior>
    Tokens:
    Test T-1: `TokenType` has exactly 19 members; `len(list(TokenType)) == 19`.
    Test T-2: `Token(type=TokenType.NUMBER, value="42", line=3, col=0, source_line="x = 42")` constructs without error and `token.line == 3`.
    Test T-3: Two `Token` instances with identical fields compare equal via `==` (dataclass __eq__).
    Test T-4: `KEYWORDS["show"] == TokenType.KEYWORD` and `KEYWORDS["function"] == TokenType.KEYWORD`.
    Test T-5: `len(KEYWORDS) == 18` (18 Atena keywords from LEX-06).
    Test T-6: `"score" not in KEYWORDS` (user-defined identifiers are NOT in the keyword map).
    Test T-7: `repr(Token(...))` contains the field names (dataclass __repr__ auto-generated).

    AST Nodes:
    Test A-1: All 22 node type names are importable from `atena.ast_nodes` in a single import statement.
    Test A-2: `Program(statements=[], line=0)` constructs without error; `prog.line == 0`.
    Test A-3: `BinOp(op="+", left=NumberLiteral(value=1, line=1), right=NumberLiteral(value=2, line=1), line=1)` constructs and `binop.op == "+"`.
    Test A-4: Two identical `Assign` instances compare equal (`==`).
    Test A-5: `IndexAccess(target=Identifier(name="items", line=2), index=NumberLiteral(value=1, line=2), line=2).index_converted == False`.
    Test A-6: `from atena.ast_nodes import Program` does NOT trigger any import of `atena.errors`, `atena.tokens`, `atena.cli`, or `atena.pipeline` (verified by checking `sys.modules` after import).
    Test A-7: `DictLiteral(pairs=[("name", StringLiteral(value="Ana", line=5))], line=5)` constructs correctly.
  </behavior>
  <implementation>
    RED phase: Write `tests/test_tokens.py` (7 tests) and `tests/test_ast_nodes.py` (7 tests). Run `pytest tests/test_tokens.py tests/test_ast_nodes.py` — all 14 must FAIL (stubs have no real bodies).

    GREEN phase — `src/atena/tokens.py`:
    - `from __future__ import annotations`
    - `import enum`
    - `from dataclasses import dataclass`
    - `class TokenType(enum.Enum)` with 19 members: `STRING = "STRING"`, `NUMBER = "NUMBER"`, `IDENTIFIER = "IDENTIFIER"`, `KEYWORD = "KEYWORD"`, `OPERATOR = "OPERATOR"`, `COMPARISON = "COMPARISON"`, `ASSIGN = "ASSIGN"`, `LPAREN = "LPAREN"`, `RPAREN = "RPAREN"`, `LBRACKET = "LBRACKET"`, `RBRACKET = "RBRACKET"`, `LBRACE = "LBRACE"`, `RBRACE = "RBRACE"`, `COMMA = "COMMA"`, `DOT = "DOT"`, `NEWLINE = "NEWLINE"`, `INDENT = "INDENT"`, `DEDENT = "DEDENT"`, `EOF = "EOF"`.
    - `@dataclass(frozen=True) class Token`: fields `type: TokenType`, `value: str`, `line: int`, `col: int`, `source_line: str`.
    - `KEYWORDS: dict[str, TokenType]` mapping all 18 Atena keywords to `TokenType.KEYWORD`. Full list: `show ask if else while repeat times and or not function return add to remove from length true false`.

    GREEN phase — `src/atena/ast_nodes.py`:
    - `from __future__ import annotations`
    - `from dataclasses import dataclass, field`
    - `@dataclass class Node`: `line: int = 0`, `source_line: str = ""` — base class for all nodes.
    - Define all 22 concrete dataclass nodes inheriting from `Node`. Each adds its structural fields. `IndexAccess` adds `index_converted: bool = False` (default False, set True by analyzer after rewrite per ARCHITECTURE.md Pattern 11). Use `field(default_factory=list)` for all list-typed fields to prevent shared mutable defaults.
    - Zero non-stdlib imports.

    REFACTOR: Verify `Token` is `frozen=True` (immutable — correct for lexer output). Verify `IndexAccess.index_converted` defaults to False. Run all 14 tests — all must PASS.

    Commits:
    1. `git commit -m "test(00-03): add failing token and ast_node contract tests"`
    2. `git commit -m "feat(00-03): implement TokenType, Token, KEYWORDS, and all 22 AST node dataclasses"`
    3. (if refactored) `git commit -m "refactor(00-03): tighten field defaults and frozen constraint"`
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lexer → Token → downstream phases | Token carries source_line text from the learner's file; downstream code reads but never executes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03-01 | Tampering | Token.source_line field | accept | source_line is read-only after construction (frozen=True dataclass); no mutation risk in data contract |
| T-00-03-02 | Information Disclosure | AST node __repr__ output | accept | repr() used for test debugging only; never surfaced to the learner; no sensitive data |
| T-00-03-03 | Denial of Service | KEYWORDS dict lookup | accept | Static dict at module load; O(1) lookup; no user-controlled growth |
</threat_model>

<verification>
```bash
cd /Users/juliorcoelho/atena-lang
pytest tests/test_tokens.py tests/test_ast_nodes.py -v
```
All 14 tests pass (exit 0).

Import isolation check:
```python
import sys
import importlib
importlib.import_module("atena.ast_nodes")
assert "atena.errors" not in sys.modules, "ast_nodes must not import errors"
assert "atena.tokens" not in sys.modules, "ast_nodes must not import tokens"
print("isolation check PASS")
```
</verification>

<success_criteria>
- `pytest tests/test_tokens.py tests/test_ast_nodes.py -v` exits 0 with all 14 tests PASSED
- `TokenType` has exactly 19 members; `KEYWORDS` has exactly 18 entries
- `Token` is a frozen dataclass (mutation raises `FrozenInstanceError`)
- All 22 AST node names importable from `atena.ast_nodes` in one import statement
- `IndexAccess` has field `index_converted: bool` defaulting to `False`
- `grep -c "^from atena\|^import atena" src/atena/tokens.py src/atena/ast_nodes.py` equals 0 (no sibling imports)
- `from atena.ast_nodes import Program` does not add `atena.errors` or `atena.tokens` to `sys.modules`
</success_criteria>

<output>
Create `.planning/phases/00-diagnostics-spine-data-contracts/00-03-SUMMARY.md` when done
</output>
