---
phase: "00"
plan: "02"
type: tdd
wave: 2
depends_on:
  - "00-01"
files_modified:
  - src/atena/errors.py
  - tests/test_errors.py
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
  - DIAG-04
  - DIAG-06
must_haves:
  truths:
    - "Given errors added on lines 4, 1, 9, the reporter outputs them sorted as line 1, line 4, line 9 (D-01, DIAG-02)"
    - "Each error prints as exactly 'Error on line {N}: {message}' followed by '  → {source_line}' (D-01, DIAG-01)"
    - "Three errors on distinct lines all appear in report() output, not just the first (DIAG-02)"
    - "Duplicate errors (same line AND same message) are collapsed to one before the cap is applied (D-09, DIAG-06)"
    - "After ~10 unique errors, report() appends '…and N more. Fix some and run again to see the rest.' and omits the excess (D-08, DIAG-06)"
    - "is_empty() returns True on a fresh ErrorCollector and False after any add() call (D-03)"
    - "No Python jargon appears in any format string inside errors.py (D-03, DIAG-03)"
    - "The error format template exists in errors.py as the ONLY place it is written (ARCHITECTURE.md single-source-of-truth contract)"
  artifacts:
    - path: "src/atena/errors.py"
      provides: "ErrorCollector class with add/is_empty/report; ERROR_CAP constant; dedup+sort+cap logic; error format template"
      exports: ["ErrorCollector"]
      contains: "Error on line"
    - path: "tests/test_errors.py"
      provides: "TDD test suite: format, sort, dedup, cap — exact string assertions against canonical exemplars"
  key_links:
    - from: "tests/test_errors.py"
      to: "src/atena/errors.py"
      via: "from atena.errors import ErrorCollector"
      pattern: "from atena\\.errors import ErrorCollector"
---

<objective>
Implement `ErrorCollector` — the cross-cutting diagnostics spine — using TDD. The class accumulates `(line, message, source_line)` records, deduplicates them, sorts by line, caps at ~10, and formats them as `Error on line {N}: {message}\n  → {source_line}`. The error format template lives here and ONLY here.

Purpose: Every later phase (Lexer through Generator) imports the same `ErrorCollector` and calls `add()`. Getting this right first means all four phases share one format and one dedup/sort/cap policy.
Output: A fully-tested `errors.py` with `ErrorCollector` and a green `tests/test_errors.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/juliorcoelho/atena-lang/.planning/PROJECT.md
@/Users/juliorcoelho/atena-lang/.planning/ROADMAP.md
@/Users/juliorcoelho/atena-lang/.planning/phases/00-diagnostics-spine-data-contracts/00-CONTEXT.md

<interfaces>
<!-- After Plan 01, the stub exists. This plan REPLACES the stub body with real implementation. -->

<!-- ErrorCollector contract (from ARCHITECTURE.md + CONTEXT.md): -->
<!--   ErrorCollector()                           — constructor, zero dependencies -->
<!--   .add(line: int, message: str, source_line: str) -> None -->
<!--   .is_empty() -> bool -->
<!--   .report() -> str   — returns the full formatted block (or empty string if no errors) -->

<!-- Canonical exemplars from CONTEXT.md <specifics> (these are EXACT expected outputs): -->
<!--   Single error format: -->
<!--     "Error on line 4: I don't know what \"score\" is yet.\n  → show score" -->
<!--   Overflow line (D-08): -->
<!--     "…and 23 more. Fix some and run again to see the rest." -->

<!-- ERROR_CAP = 10  (D-08: ~10 errors shown; remainder collapsed) -->
<!-- Dedup rule (D-09): same (line, message) pair → collapse to one BEFORE cap -->
<!-- Sort rule: stable sort by line number ascending; same-line errors keep insertion order -->
</interfaces>
</context>

<feature>
  <name>ErrorCollector: collect, dedup, sort, cap, format</name>
  <files>src/atena/errors.py, tests/test_errors.py</files>
  <behavior>
    Test 1 — Empty collector: `ErrorCollector().is_empty()` returns `True`; `ErrorCollector().report()` returns `""`.
    Test 2 — Single error format: add line=4, message='I don\'t know what "score" is yet.', source_line='show score'; `report()` returns exactly `'Error on line 4: I don\'t know what "score" is yet.\n  → show score'`.
    Test 3 — Multi-error sort: add three errors on lines 9, 1, 4; `report()` presents line 1 first, line 4 second, line 9 third.
    Test 4 — All three collected: with three errors on distinct lines, `report()` contains all three `Error on line …` headers.
    Test 5 — Dedup: add identical (line=3, message='same', source='x') twice; `report()` contains exactly one `Error on line 3` block.
    Test 6 — Dedup different message same line: add (line=3, message='A', source='x') and (line=3, message='B', source='x'); both appear in `report()`.
    Test 7 — Cap: add 14 distinct errors; `report()` contains exactly 10 `Error on line` blocks plus one trailing line matching `'…and 4 more. Fix some and run again to see the rest.'`; `report()` does NOT contain an 11th `Error on line`.
    Test 8 — Dedup before cap: add 8 unique errors plus 5 duplicates of error #1; effective unique count = 8; `report()` shows all 8 (no overflow line, because 8 < 10).
    Test 9 — is_empty() after add: `is_empty()` returns `False` after any `add()` call.
    Test 10 — No jargon: scan `errors.py` source text for forbidden words: "token", "AST", "DEDENT", "arity", "NoneType"; none must appear in any string literal.
  </behavior>
  <implementation>
    RED phase: Write `tests/test_errors.py` with all 10 tests above. Run `pytest tests/test_errors.py` — every test must FAIL (the stub has no implementation).

    GREEN phase: Replace the stub body in `src/atena/errors.py` with the real implementation:
    - Internal `_ErrorRecord` (namedtuple or dataclass with fields `line: int`, `message: str`, `source_line: str`)
    - `ErrorCollector.__init__`: `self._records: list[_ErrorRecord] = []`
    - `ErrorCollector.add(line, message, source_line)`: append a new `_ErrorRecord` unconditionally
    - `ErrorCollector.is_empty()`: `return len(self._records) == 0`
    - `ErrorCollector.report()`: (1) dedup by (line, message) preserving first occurrence order, (2) stable-sort by line ascending, (3) split at `ERROR_CAP = 10`, (4) format each of the first 10 as `f"Error on line {r.line}: {r.message}\n  → {r.source_line}"`, (5) if overflow: append `f"…and {overflow_count} more. Fix some and run again to see the rest."`, (6) join with `"\n\n"` separator between error blocks, trailing overflow line joined with `"\n"` after the last error block.
    - Module constant `ERROR_CAP: int = 10` at module top.
    - Keep `suggest()` as a stub — it is implemented in Plan 04.

    REFACTOR: Confirm report() separator — each `Error on line …\n  → …` block is separated from the next by a blank line (double newline); the overflow line is on its own line immediately after the last error block. Run `pytest tests/test_errors.py` — all 10 must PASS.

    Commit sequence (one commit per phase, per CLAUDE.md TDD process):
    1. `git commit -m "test(00-02): add failing ErrorCollector tests"`
    2. `git commit -m "feat(00-02): implement ErrorCollector with format/dedup/sort/cap"`
    3. (if refactored) `git commit -m "refactor(00-02): clean up ErrorCollector report formatting"`
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any phase → ErrorCollector | Phases call add() with line numbers and message strings from untrusted source text |
| ErrorCollector → terminal output | report() string is printed to stdout; must never include Python exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | Information Disclosure | report() output | mitigate | Verify no Python jargon ("NoneType", "traceback", "Traceback") appears in any format string — covered by Test 10; keep `suggest()` as stub so no exception path is live yet |
| T-00-02-02 | Denial of Service | unbounded _records list | mitigate | ERROR_CAP enforced at report() render time; add() is uncapped (phases append freely), but report() only renders ≤10 — consistent with D-08; pathological input with 10,000 errors does not crash, just caps output |
| T-00-02-03 | Tampering | source_line text injected into error output | accept | ErrorCollector is an offline CLI tool; source_line comes from the learner's own .atena file, not a network; no injection risk in a local teaching tool |
</threat_model>

<verification>
```bash
cd /Users/juliorcoelho/atena-lang
pytest tests/test_errors.py -v
```
All 10 tests pass (exit 0).

Exact-string spot-check (execute in Python):
```python
from atena.errors import ErrorCollector
ec = ErrorCollector()
ec.add(4, 'I don\'t know what "score" is yet.', 'show score')
assert ec.report() == 'Error on line 4: I don\'t know what "score" is yet.\n  → show score'
print("format check PASS")
```
</verification>

<success_criteria>
- `pytest tests/test_errors.py -v` exits 0 with all 10 tests PASSED
- `ErrorCollector().report()` for a single error equals exactly `'Error on line {N}: {message}\n  → {source_line}'` with two leading spaces before the arrow
- `ErrorCollector().report()` for 14 distinct errors shows exactly 10 error blocks plus one overflow line matching `'…and 4 more. Fix some and run again to see the rest.'`
- Dedup before cap: 8 unique + 5 duplicates → 8 shown, no overflow line
- `grep -c "Error on line" src/atena/errors.py` equals 1 (single format template, not duplicated)
- `grep -v "^#" src/atena/errors.py | grep -c "NoneType\|traceback\|Traceback\|AST\|DEDENT\|arity"` equals 0
- Three red→green→refactor commits exist on the feature branch
</success_criteria>

<output>
Create `.planning/phases/00-diagnostics-spine-data-contracts/00-02-SUMMARY.md` when done
</output>
