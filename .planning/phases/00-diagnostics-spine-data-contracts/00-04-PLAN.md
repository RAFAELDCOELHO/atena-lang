---
phase: "00"
plan: "04"
type: tdd
wave: 3
depends_on:
  - "00-02"
files_modified:
  - src/atena/errors.py
  - tests/test_errors.py
autonomous: true
requirements:
  - DIAG-04
  - DIAG-05
  - DIAG-06
must_haves:
  truths:
    - "A typo like 'scr' with candidate 'score' produces 'Did you mean \"score\"?' appended to the error message (D-04, DIAG-05)"
    - "A typo like 'shwo' with candidate 'show' produces 'Did you mean \"show\"?' (D-04)"
    - "A case-only mismatch like 'Score' vs defined 'score' produces 'Did you mean \"score\"? Names must match capitalization exactly.' (D-06)"
    - "A wild miss like 'banana' with no close candidate produces NO suggestion (D-04 silence-on-bad-guess rule)"
    - "Only ONE best suggestion is returned even when multiple candidates are close; ties broken deterministically (D-07)"
    - "The candidate set is passed in by the caller — the engine accepts any arbitrary list, not a hardcoded set (D-05)"
    - "Keywords are available as a candidate source via a constant or utility in errors.py (D-05)"
    - "suggest() lives in errors.py alongside ErrorCollector — no separate suggestion module (ARCHITECTURE.md single-source-of-truth)"
  artifacts:
    - path: "src/atena/errors.py"
      provides: "suggest(name, candidates) function using difflib.get_close_matches + case-only detection; ATENA_KEYWORDS list for use by phases"
      exports: ["ErrorCollector", "suggest", "ATENA_KEYWORDS"]
      contains: "difflib"
    - path: "tests/test_errors.py"
      provides: "Extended test suite: suggest() tests for typos, case mismatch, wild miss, single-best, arbitrary candidate set"
  key_links:
    - from: "tests/test_errors.py"
      to: "src/atena/errors.py"
      via: "from atena.errors import suggest, ATENA_KEYWORDS"
      pattern: "from atena\\.errors import"
---

<objective>
Implement the "Did you mean?" suggestion engine inside `errors.py`, using TDD. The `suggest(name, candidates)` function returns either a suggestion string (ready to append to an error message) or `None` when no close match exists. It uses `difflib.get_close_matches` (stdlib) for similarity, with a case-only mismatch detected first and called out explicitly per D-06.

Purpose: Every phase that reports unknown-name errors passes its known names to `suggest()` and appends the result (if any) to the error message. One implementation, consistent behavior, first-person teaching voice.
Output: A fully-tested `suggest()` function and `ATENA_KEYWORDS` constant in `errors.py`; extended green `tests/test_errors.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/juliorcoelho/atena-lang/.planning/PROJECT.md
@/Users/juliorcoelho/atena-lang/.planning/ROADMAP.md
@/Users/juliorcoelho/atena-lang/.planning/phases/00-diagnostics-spine-data-contracts/00-CONTEXT.md
@/Users/juliorcoelho/atena-lang/.planning/phases/00-diagnostics-spine-data-contracts/00-02-SUMMARY.md

<interfaces>
<!-- suggest() contract (from D-04, D-05, D-06, D-07 in CONTEXT.md): -->
<!--                                                                    -->
<!--   suggest(name: str, candidates: list[str]) -> str | None         -->
<!--                                                                    -->
<!--   Returns a READY-TO-APPEND string or None.                       -->
<!--   Caller appends to the error message (or not if None).           -->
<!--                                                                    -->
<!-- Return value shapes (from CONTEXT.md <specifics>):                -->
<!--   Case-only mismatch (D-06):                                      -->
<!--     'Did you mean "score"? Names must match capitalization exactly.'  -->
<!--   Typo / non-case difference (D-04):                              -->
<!--     'Did you mean "score"?'                                       -->
<!--   No close match (D-04 silence rule):                             -->
<!--     None                                                          -->
<!--                                                                    -->
<!-- Algorithm (Claude's Discretion — chosen here):                    -->
<!--   1. Case-only check FIRST:                                       -->
<!--      if name.lower() == candidate.lower() for any candidate       -->
<!--      → return D-06 form (teach the rule; always suggest)          -->
<!--   2. difflib.get_close_matches(name, candidates, n=1, cutoff=0.6) -->
<!--      (cutoff 0.6 = ~60% similarity; length-aware because          -->
<!--       difflib uses SequenceMatcher ratio, which scales with        -->
<!--       length — a 3-char match needs 2/3 chars correct = 66%)      -->
<!--   3. If match found → 'Did you mean "{match}"?'                   -->
<!--   4. No match → None                                              -->
<!--                                                                    -->
<!-- Tie-breaking (D-07): get_close_matches already returns at most    -->
<!-- n=1; when two candidates tie in ratio, difflib returns the one    -->
<!-- that appears first in the list. Caller orders candidates           -->
<!-- first-defined then alphabetical to achieve deterministic results.  -->
<!--                                                                    -->
<!-- ATENA_KEYWORDS: list[str] constant — same 18 words as KEYWORDS    -->
<!-- dict in tokens.py but as a plain list so phases can easily extend -->
<!-- it: user_candidates = list(ATENA_KEYWORDS) + known_vars           -->
<!--                                                                    -->
<!-- The suggest() stub from Plan 01 is replaced here.                 -->
</interfaces>
</context>

<feature>
  <name>"Did you mean?" suggestion engine</name>
  <files>src/atena/errors.py, tests/test_errors.py</files>
  <behavior>
    Test S-1 — Typo caught: `suggest("scr", ["score", "show", "ask"])` returns `'Did you mean "score"?'`
    Test S-2 — Keyword typo caught: `suggest("shwo", list(ATENA_KEYWORDS))` returns `'Did you mean "show"?'`
    Test S-3 — Wild miss silenced: `suggest("banana", ["score", "show", "ask"])` returns `None`
    Test S-4 — Case-only mismatch: `suggest("Score", ["score", "show", "ask"])` returns `'Did you mean "score"? Names must match capitalization exactly.'`
    Test S-5 — Case-only mismatch takes priority over fuzzy: even if "score" is also a close fuzzy match, when it is a case-only match the D-06 form is returned (test with `suggest("Score", ["score"])`)
    Test S-6 — Single best result: `suggest("scre", ["score", "screech"])` returns exactly ONE suggestion (the closer one, `'Did you mean "score"?'`)
    Test S-7 — Deterministic tie-breaking: `suggest("ab", ["ac", "ad"])` returns the same result on repeated calls (no randomness)
    Test S-8 — Empty candidate list: `suggest("xyz", [])` returns `None` (no crash)
    Test S-9 — Exact match not suggested: `suggest("score", ["score"])` returns `None` (no suggestion needed when name is already correct)
    Test S-10 — ATENA_KEYWORDS contains all 18 Atena keywords: `len(ATENA_KEYWORDS) == 18` and `"show" in ATENA_KEYWORDS` and `"function" in ATENA_KEYWORDS`
    Test S-11 — Case-only check is case-insensitive: `suggest("SHOW", list(ATENA_KEYWORDS))` returns `'Did you mean "show"? Names must match capitalization exactly.'`
  </behavior>
  <implementation>
    RED phase: Add 11 new tests (S-1 through S-11) to `tests/test_errors.py`. Import `suggest` and `ATENA_KEYWORDS` from `atena.errors`. Run `pytest tests/test_errors.py` — existing 10 tests (from Plan 02) must still pass; the 11 new tests must FAIL.

    GREEN phase: Replace the stub `suggest()` in `src/atena/errors.py` with the real implementation:
    - `import difflib` at the top of the file (stdlib only).
    - `ATENA_KEYWORDS: list[str] = ["show", "ask", "if", "else", "while", "repeat", "times", "and", "or", "not", "function", "return", "add", "to", "remove", "from", "length", "true", "false"]` — 18 items, same set as `tokens.KEYWORDS` but maintained independently as a plain list (errors.py must not import tokens.py per the dependency-free contract: suggest is a utility for ANY phase, not just the lexer).

    Wait — ARCHITECTURE.md says errors.py is the single source of truth for error formatting, and suggest() lives there. But `tokens.py` also defines KEYWORDS. These are maintained independently — the keyword list is short and stable; accept the duplication to preserve the zero-sibling-import rule on tokens.py and ast_nodes.py.

    - `def suggest(name: str, candidates: list[str]) -> str | None`:
      1. If `candidates` is empty, return `None`.
      2. If `name in candidates`, return `None` (exact match, no suggestion needed).
      3. Case-only check: for each candidate, if `name.lower() == candidate.lower()`, return `f'Did you mean "{candidate}"? Names must match capitalization exactly.'` (use the FIRST such candidate found).
      4. Fuzzy check: `matches = difflib.get_close_matches(name, candidates, n=1, cutoff=0.6)`; if matches, return `f'Did you mean "{matches[0]}"?'`.
      5. Return `None`.

    REFACTOR: Confirm `import difflib` is the only new import. Verify no sibling-module imports added. Run all 21 tests — all must PASS (10 original + 11 new).

    Commits:
    1. `git commit -m "test(00-04): add failing suggest() and ATENA_KEYWORDS tests"`
    2. `git commit -m "feat(00-04): implement suggest() with case-only detection and difflib fuzzy matching"`
    3. (if refactored) `git commit -m "refactor(00-04): simplify suggest() early-return path"`
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase error call → suggest() | name and candidates come from source text (learner's identifiers) and phase symbol tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-04-01 | Denial of Service | suggest() with enormous candidate list | mitigate | difflib.get_close_matches is O(n * m) where n=candidates, m=name length; for a teaching transpiler, the candidate set is bounded by the number of variables + 18 keywords in one program — practically never more than a few hundred; no explicit cap needed, but document the bound |
| T-00-04-02 | Information Disclosure | suggestion string in error output | accept | suggest() returns only names that already exist in the learner's own symbol table or Atena's keyword list — no sensitive information disclosed |
| T-00-04-03 | Tampering | ATENA_KEYWORDS constant mutated at runtime | accept | List is defined at module level; callers who extend it via `list(ATENA_KEYWORDS) + extra` get a copy; no defensive copy needed for this use pattern |
</threat_model>

<verification>
```bash
cd /Users/juliorcoelho/atena-lang
pytest tests/test_errors.py -v
```
All 21 tests pass (10 from Plan 02 + 11 new from this plan). Exit 0.

Canonical exemplar spot-check:
```python
from atena.errors import suggest, ATENA_KEYWORDS
assert suggest("scr", ["score", "show"]) == 'Did you mean "score"?'
assert suggest("Score", ["score"]) == 'Did you mean "score"? Names must match capitalization exactly.'
assert suggest("banana", ["score", "show"]) is None
assert suggest("shwo", list(ATENA_KEYWORDS)) == 'Did you mean "show"?'
print("all exemplar checks PASS")
```
</verification>

<success_criteria>
- `pytest tests/test_errors.py -v` exits 0 with all 21 tests PASSED (10 from Plan 02 + 11 new)
- `suggest("scr", ["score", "show", "ask"])` returns exactly `'Did you mean "score"?'`
- `suggest("Score", ["score"])` returns exactly `'Did you mean "score"? Names must match capitalization exactly.'`
- `suggest("banana", ["score", "show", "ask"])` returns `None`
- `suggest("shwo", list(ATENA_KEYWORDS))` returns `'Did you mean "show"?'`
- `len(ATENA_KEYWORDS) == 18` (verify with `python -c "from atena.errors import ATENA_KEYWORDS; assert len(ATENA_KEYWORDS)==18"`)
- `grep -c "^from atena\|^import atena" src/atena/errors.py` equals 0 (no sibling imports — `import difflib` is the only new import, which is stdlib)
</success_criteria>

<output>
Create `.planning/phases/00-diagnostics-spine-data-contracts/00-04-SUMMARY.md` when done
</output>
