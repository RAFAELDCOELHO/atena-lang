---
phase: 03-semantic-analyzer
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/atena/analyzer.py
  - tests/test_analyzer.py
autonomous: true
requirements:
  - SEM-01
  - SEM-02
  - SEM-03
  - SEM-04
  - SEM-05

must_haves:
  truths:
    - "str() is injected for string+number and string+boolean (right or left side as appropriate)"
    - "number+number and string+string are left untouched (no FunctionCall wrap injected)"
    - "1+\"x\" wraps the left NumberLiteral in FunctionCall(name=\"str\")"
    - "\"a\"+1+2 chains correctly — both 1 and 2 are wrapped — no runtime TypeError"
    - "A literal items[1] has index.value==0 and index_converted==True after analysis"
    - "Nested grid[2][3] becomes grid[1][2] (not grid[0][1]) — no double-shift"
    - "items[0] produces an error containing 'start at 1, not 0'"
    - "items[-3] produces a distinct error containing 'no negative positions'"
    - "items[i] (variable index) has index replaced by FunctionCall(name='_atena_index', args=[Identifier('i')])"
    - "Disallowed + combinations (list+number, bool+bool, etc.) produce plain-English 'can\\'t add' errors"
    - "All Layer 1 A1_ tests that cover SEM-01..SEM-05 pass GREEN"
    - "All Layer 2 A2_ tests that cover SEM-01..SEM-05 pass GREEN"
  artifacts:
    - path: "src/atena/analyzer.py"
      provides: "visit_BinOp, visit_IndexAccess, visit_NumberLiteral, visit_StringLiteral, visit_BoolLiteral, visit_ListLiteral, visit_DictLiteral, visit_Show, visit_Repeat, visit_Assign (value visit only), visit_If, visit_While — all with real logic"
      contains: "_COERCE_TABLE"
    - path: "tests/test_analyzer.py"
      provides: "All A1 and A2 test stubs for SEM-01..SEM-05 now passing"
  key_links:
    - from: "visit_BinOp"
      to: "_COERCE_TABLE"
      via: "_COERCE_TABLE.get((left_type, right_type))"
      pattern: "_COERCE_TABLE"
    - from: "visit_IndexAccess"
      to: "IndexAccess.index_converted"
      via: "idempotency guard at method entry"
      pattern: "index_converted"
    - from: "visit_IndexAccess"
      to: "FunctionCall(name='_atena_index')"
      via: "dynamic index routing for non-literal indices"
      pattern: "_atena_index"
---

<objective>
Implement the expression-level semantic transforms: type inference, str() coercion
injection for `+`, the "Cannot combine" error for disallowed combinations, and the
1-indexed-to-0-indexed rewrite for list access (literal fold + dynamic helper routing).

This plan turns the A1 and A2 test stubs for SEM-01 through SEM-05 GREEN while
leaving the scope/arity stubs (SEM-06/07 — Plan 03) still RED.

Purpose: The analyzer must own ALL semantic decisions so the Phase-4 generator
can emit verbatim with zero re-derivation. This plan wires the coercion and index
decisions.

Output: visit_BinOp (coercion + error), visit_IndexAccess (rewrite + helper routing),
all literal visitor type returns, and the _COERCE_TABLE + _HUMAN_TYPE constants fully
populated.
</objective>

<execution_context>
@/Users/juliorcoelho/atena-lang/.planning/phases/03-semantic-analyzer/03-PATTERNS.md
@/Users/juliorcoelho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliorcoelho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/juliorcoelho/atena-lang/.planning/PROJECT.md
@/Users/juliorcoelho/atena-lang/.planning/ROADMAP.md
@/Users/juliorcoelho/atena-lang/.planning/phases/03-semantic-analyzer/03-CONTEXT.md
@/Users/juliorcoelho/atena-lang/.planning/phases/03-semantic-analyzer/03-01-SUMMARY.md

<interfaces>
<!-- Contracts and patterns the executor must follow. -->

Type lattice (from D-01 and 03-PATTERNS.md):
  "str", "number", "bool", "list", "dict", "unknown"

_HUMAN_TYPE (complete map — populate in analyzer.py):
  {"str": "text", "number": "number", "bool": "true/false",
   "list": "list", "dict": "dictionary", "unknown": "unknown"}

_COERCE_TABLE (complete — populate in analyzer.py):
  ("str",    "str"):     "no_coerce"
  ("str",    "number"):  "coerce_right"    # wrap number in str()
  ("str",    "bool"):    "coerce_right"    # wrap bool in str()
  ("number", "str"):     "coerce_left"     # wrap number in str()
  ("number", "number"):  "no_coerce"
  ("number", "bool"):    "error"
  ("bool",   "str"):     "coerce_left"     # wrap bool in str()
  ("bool",   "number"):  "error"
  ("bool",   "bool"):    "error"
  ("list",   ANY):       "error"           # except list+list which is also error
  ("dict",   ANY):       "error"
  (ANY, "list"):         "error"
  (ANY, "dict"):         "error"
  # When either side is "unknown": route through _atena_concat (D-02)
  # This is checked BEFORE table lookup (short-circuit)

Idempotency guard in visit_IndexAccess (MUST be first check):
  if node.index_converted:
      self._visit(node.index)
      return "unknown"

Literal-type returns (set these in the respective visit_* stubs):
  visit_NumberLiteral → return "number"
  visit_StringLiteral → return "str"
  visit_BoolLiteral   → return "bool"
  visit_ListLiteral   → visit each element; return "list"
  visit_DictLiteral   → visit each value; return "dict"

visit_Show(node) → self._visit(node.value); return "unknown"
visit_Repeat(node) → self._visit(node.count); visit each body stmt; return "unknown"
visit_If(node) → visit condition, then_body, else_body; return "unknown"
visit_While(node) → visit condition, body; return "unknown"
visit_Assign(node) — Plan 02 visits node.value for type; symbol table registration is Plan 03

visit_BinOp key logic (per 03-PATTERNS.md §BinOp coercion):
  1. left_type = self._visit(node.left)
  2. right_type = self._visit(node.right)
  3. if node.op != "+": return "unknown"  (non-+ not type-checked, D-04)
  4. if "unknown" in (left_type, right_type): inject _atena_concat(left, right) helper (D-02)
  5. outcome = _COERCE_TABLE.get((left_type, right_type), "error")
  6. dispatch on outcome: error→add message; coerce_right→wrap node.right; coerce_left→wrap node.left
  7. return "str" after coerce, left_type if no_coerce, "unknown" after error

_atena_concat injection (D-02): when either side is unknown, replace BinOp fields:
  helper = FunctionCall(name="_atena_concat", args=[node.left, node.right], ...)
  node.left = helper
  node.right = NumberLiteral(value=0)  # sentinel — codegen reads node.left for _atena_concat
  NOTE: simpler alternative from 03-PATTERNS.md — planner chose node-injection via replacing
  node.left with the helper; the generator checks isinstance(node.left, FunctionCall) and name.
  Both left/right of a "replaced" BinOp are visible. PREFER: store helper as node.left,
  set a flag on node. OR simpler: since codegen must know, store as a top-level node replacement
  via visiting parent. ACTUAL DECISION (Claude's discretion — pick simplest):
  Replace node.left = FunctionCall(_atena_concat, [original_left, original_right])
  and set node.right = original_right's sentinel (NumberLiteral value=0, line/source same as node).
  Codegen in Phase 4 will detect isinstance(binop.left, FunctionCall) with name _atena_concat
  and emit _atena_concat(a, b) instead of a + b. Document the convention in a comment.

visit_IndexAccess key logic (per 03-PATTERNS.md §IndexAccess, D-05, D-06):
  1. self._visit(node.target)
  2. if node.index_converted: self._visit(node.index); return "unknown"   # idempotency
  3. if isinstance(node.index, NumberLiteral):
       if node.index.value == 0: error "Lists in Atena start at 1, not 0."
       else: node.index.value -= 1; node.index_converted = True
  4. elif isinstance(node.index, UnaryOp) and node.index.op == "-" and isinstance(node.index.operand, NumberLiteral):
       error "Atena lists count from 1 — there are no negative positions. The last item is at length, not -1."
  5. else (dynamic — variable, expression, FunctionCall result):
       helper = FunctionCall(name="_atena_index", args=[node.index], line=node.line, source_line=node.source_line)
       node.index = helper
       node.index_converted = True
  return "unknown"

Error message for disallowed +, using _HUMAN_TYPE:
  f"I can't add a {_HUMAN_TYPE[left_type]} and a {_HUMAN_TYPE[right_type]} together — try making them the same kind first."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate _COERCE_TABLE, _HUMAN_TYPE, and literal visitor types</name>
  <files>src/atena/analyzer.py</files>
  <read_first>
    - src/atena/analyzer.py (current skeleton from Plan 01 — read completely to see current state)
    - .planning/phases/03-semantic-analyzer/03-PATTERNS.md (§BinOp coercion pattern, _COERCE_TABLE, _HUMAN_TYPE)
    - .planning/phases/03-semantic-analyzer/03-CONTEXT.md (D-01, D-02, D-04 — coercion rules)
    - src/atena/ast_nodes.py (NumberLiteral, StringLiteral, BoolLiteral, ListLiteral, DictLiteral)
  </read_first>
  <behavior>
    - _HUMAN_TYPE maps all six lattice members to human-readable labels (str→"text", number→"number", bool→"true/false", list→"list", dict→"dictionary", unknown→"unknown")
    - _COERCE_TABLE is fully populated: every (left, right) pair that can arise from str/number/bool/list/dict maps to "no_coerce", "coerce_right", "coerce_left", or "error"
    - Any unknown-side check is handled as a short-circuit BEFORE the table lookup in visit_BinOp (not in the table itself)
    - visit_NumberLiteral returns "number"
    - visit_StringLiteral returns "str"
    - visit_BoolLiteral returns "bool"
    - visit_ListLiteral visits each element and returns "list"
    - visit_DictLiteral visits each value in pairs and returns "dict"
    - visit_Show visits node.value and returns "unknown"
    - visit_If visits condition, then_body statements, else_body statements; returns "unknown"
    - visit_While visits condition and body statements; returns "unknown"
    - visit_Repeat visits count and body statements; returns "unknown"
    - visit_Assign visits node.value (symbol table registration deferred to Plan 03); returns "unknown"
    - visit_Return visits node.value; returns "unknown"
    - visit_ListAdd visits node.value; returns "unknown"
    - visit_ListRemove visits node.value; returns "unknown"
    - visit_DotAccess visits node.target; returns "unknown"
  </behavior>
  <action>
    Edit src/atena/analyzer.py to:

    1. Replace the empty _HUMAN_TYPE and _COERCE_TABLE stubs with the complete populated versions per the interfaces block above. The _COERCE_TABLE must cover all (str/number/bool/list/dict) x (str/number/bool/list/dict) combinations — for list and dict on either side the outcome is always "error". Use the exact key-value pairs from the interfaces block.

    2. Update visit_NumberLiteral to return "number", visit_StringLiteral to return "str", visit_BoolLiteral to return "bool".

    3. Update visit_ListLiteral: call self._visit(elem) for each element in node.elements; return "list".

    4. Update visit_DictLiteral: call self._visit(val) for each (key, val) in node.pairs; return "dict".

    5. Update visit_Show: self._visit(node.value); return "unknown".

    6. Update visit_If: self._visit(node.condition); for stmt in node.then_body: self._visit(stmt); for stmt in node.else_body: self._visit(stmt); return "unknown".

    7. Update visit_While: self._visit(node.condition); for stmt in node.body: self._visit(stmt); return "unknown".

    8. Update visit_Repeat: self._visit(node.count); for stmt in node.body: self._visit(stmt); return "unknown".

    9. Update visit_Assign: inferred = self._visit(node.value); return "unknown" (symbol table registration is Plan 03 — for now Assign visits but doesn't register).

    10. Update visit_Return: self._visit(node.value); return "unknown".

    11. Update visit_ListAdd: self._visit(node.value); return "unknown".

    12. Update visit_ListRemove: self._visit(node.value); return "unknown".

    13. Update visit_DotAccess: self._visit(node.target); return "unknown".
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -c "
from atena.analyzer import SemanticAnalyzer, _COERCE_TABLE, _HUMAN_TYPE
assert ('str','number') in _COERCE_TABLE
assert _COERCE_TABLE[('str','number')] == 'coerce_right'
assert _COERCE_TABLE[('number','number')] == 'no_coerce'
assert _COERCE_TABLE[('bool','bool')] == 'error'
assert _HUMAN_TYPE['str'] == 'text'
print('constants OK')
"</automated>
  </verify>
  <done>_COERCE_TABLE covers all 25+ known-type pairs; _HUMAN_TYPE maps all 6 lattice members; all structural visitor stubs (Show, If, While, Repeat, Assign, Return, ListAdd, ListRemove, DotAccess) visit their children and return correct types.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement visit_BinOp (coercion + "Cannot combine" error)</name>
  <files>src/atena/analyzer.py</files>
  <read_first>
    - src/atena/analyzer.py (current state after Task 1)
    - .planning/phases/03-semantic-analyzer/03-PATTERNS.md (§BinOp coercion pattern — full code excerpt)
    - .planning/phases/03-semantic-analyzer/03-CONTEXT.md (D-01, D-02, D-03, D-04, SEM-01/02 specifics)
    - src/atena/ast_nodes.py (BinOp, FunctionCall, UnaryOp fields)
    - src/atena/errors.py (ErrorCollector.add signature)
  </read_first>
  <behavior>
    - visit_BinOp with op != "+" returns "unknown" with no coercion (D-04)
    - "str" + "str": BinOp unchanged, returns "str"
    - "number" + "number": BinOp unchanged, returns "number"
    - "str" + "number": node.right replaced with FunctionCall(name="str", args=[original_right]); returns "str"
    - "number" + "str": node.left replaced with FunctionCall(name="str", args=[original_left]); returns "str"
    - "str" + "bool": node.right replaced with FunctionCall(name="str", args=[original_right]); returns "str"
    - "bool" + "str": node.left replaced with FunctionCall(name="str", args=[original_left]); returns "str"
    - "number" + "bool" or "bool" + "number": error added with "can't add" message; returns "unknown"
    - "bool" + "bool": error added; returns "unknown"
    - "list" + anything or "dict" + anything: error added; returns "unknown"
    - either side "unknown": node.left replaced with FunctionCall(name="_atena_concat", args=[original_left, original_right]); node.right set to sentinel NumberLiteral(value=0); returns "unknown" (D-02)
    - "a" + 1 + 2 chain: bottom-up evaluation means ("a"+1) result is "str", so ("str"+2) correctly coerce_rights the 2
    - Error message uses _HUMAN_TYPE for human-readable type names in source order
    - Error is added via self._errors.add(node.line, message, node.source_line)
  </behavior>
  <action>
    Edit visit_BinOp in src/atena/analyzer.py:

    1. left_type = self._visit(node.left)
    2. right_type = self._visit(node.right)
    3. if node.op != "+": return "unknown"   # Non-+ not type-checked (D-04)
    4. BEFORE table lookup: if left_type == "unknown" or right_type == "unknown":
         inject _atena_concat helper (D-02):
           orig_left = node.left
           orig_right = node.right
           helper = FunctionCall(name="_atena_concat", args=[orig_left, orig_right],
                                 line=node.line, source_line=node.source_line)
           node.left = helper
           node.right = NumberLiteral(value=0, line=node.line, source_line=node.source_line)
         return "unknown"
    5. outcome = _COERCE_TABLE.get((left_type, right_type), "error")
    6. if outcome == "error":
         msg = (f"I can't add a {_HUMAN_TYPE.get(left_type, left_type)} and a "
                f"{_HUMAN_TYPE.get(right_type, right_type)} together "
                "— try making them the same kind first.")
         self._errors.add(node.line, msg, node.source_line)
         return "unknown"
    7. if outcome == "coerce_right":
         node.right = FunctionCall(name="str", args=[node.right],
                                   line=node.right.line, source_line=node.right.source_line)
         return "str"
    8. if outcome == "coerce_left":
         node.left = FunctionCall(name="str", args=[node.left],
                                  line=node.left.line, source_line=node.left.source_line)
         return "str"
    9. # outcome == "no_coerce"
       return left_type   # both same type, result is that type

    Result-type propagation for chains: because visit_BinOp returns "str" after a coerce, and
    _visit is called bottom-up, the outer "+" in ("a"+1)+2 will see left_type="str" (the result
    of the inner BinOp) and right_type="number", correctly coercing the 2. No special chain logic needed.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -m pytest tests/test_analyzer.py -k "A1_str_coerce or A1_string_concat or A1_number_plus or A1_str_coerce_bool or A2_cannot" --tb=short -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - test_A1_string_concat_no_coerce passes (string+string unchanged)
    - test_A1_str_coerce_number_rhs passes (right wrapped in str())
    - test_A1_str_coerce_number_lhs passes (left wrapped in str())
    - test_A1_number_plus_number_no_coerce passes (no wrap)
    - test_A1_str_coerce_bool_rhs passes (bool wrapped in str())
    - test_A2_cannot_combine_number_bool passes ("can't add" in report)
    - test_Ax_chain_coercion_correct passes (ec.is_empty() and coercion nodes injected for "a"+1+2)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement visit_IndexAccess (1→0 rewrite + bounds errors + dynamic helper)</name>
  <files>src/atena/analyzer.py</files>
  <read_first>
    - src/atena/analyzer.py (current state after Task 2)
    - .planning/phases/03-semantic-analyzer/03-PATTERNS.md (§IndexAccess rewrite pattern — full code excerpt)
    - .planning/phases/03-semantic-analyzer/03-CONTEXT.md (D-05, D-06, SEM-03/04/05 specifics)
    - src/atena/ast_nodes.py (IndexAccess fields: target, index, index_converted; UnaryOp fields; FunctionCall fields; NumberLiteral.value)
    - .planning/research/PITFALLS.md (Pitfall 5 — variable index shift; Pitfall 6 — double-shift)
  </read_first>
  <behavior>
    - items[1]: index.value becomes 0, index_converted becomes True; ec.is_empty() True
    - items[2]: index.value becomes 1, index_converted True
    - grid[2][3]: outer IndexAccess index.value==1 (index_converted True), inner IndexAccess index.value==2 (index_converted True) — exactly the nested idempotency case
    - items[0]: error "Lists in Atena start at 1, not 0." added; index NOT rewritten (still 0); index_converted remains False
    - items[-3]: UnaryOp("-", NumberLiteral(3)) detected; error "Atena lists count from 1 — there are no negative positions. The last item is at length, not -1." added; index NOT rewritten
    - items[i] (Identifier): node.index replaced with FunctionCall(name="_atena_index", args=[Identifier("i")]); index_converted True
    - items[i+1] (BinOp): same dynamic path — replaced with FunctionCall(name="_atena_index")
    - If node.index_converted already True when visit_IndexAccess called: just self._visit(node.index); return immediately (idempotency, PITFALLS 6)
    - node.target is visited first (recursion enables nested: grid[2][3] visits outer then inner)
    - return "unknown" in all cases
  </behavior>
  <action>
    Edit visit_IndexAccess in src/atena/analyzer.py per the exact pattern from 03-PATTERNS.md §IndexAccess:

    1. self._visit(node.target)   # visits inner subscript first for nested access (bottom-up)

    2. if node.index_converted:   # idempotency guard (PITFALLS 6, D-05)
         self._visit(node.index)
         return "unknown"

    3. if isinstance(node.index, NumberLiteral):
         if node.index.value == 0:
           self._errors.add(node.line, "Lists in Atena start at 1, not 0.", node.source_line)
           # do NOT rewrite; do NOT set index_converted
         elif node.index.value < 0:
           # This case shouldn't arise from the parser (negatives parse as UnaryOp),
           # but guard defensively
           self._errors.add(node.line,
             "Atena lists count from 1 — there are no negative positions. The last item is at length, not -1.",
             node.source_line)
         else:
           node.index.value -= 1    # fold: 1-based → 0-based in place
           node.index_converted = True

    4. elif (isinstance(node.index, UnaryOp)
             and node.index.op == "-"
             and isinstance(node.index.operand, NumberLiteral)):
         # Literal negative index (D-06): distinct message
         self._errors.add(node.line,
           "Atena lists count from 1 — there are no negative positions. The last item is at length, not -1.",
           node.source_line)
         # do NOT rewrite; do NOT set index_converted

    5. else:
         # Dynamic index: Identifier, BinOp, FunctionCall result, or any non-literal (D-05)
         orig_index = node.index
         helper = FunctionCall(
           name="_atena_index",
           args=[orig_index],
           line=node.line,
           source_line=node.source_line,
         )
         node.index = helper
         node.index_converted = True

    6. return "unknown"

    Note: for nested grid[2][3] — the parser builds IndexAccess(target=IndexAccess(target=grid, index=NumberLiteral(3)), index=NumberLiteral(2)). When visit_IndexAccess fires on the outer node, step 1 calls self._visit(inner_IndexAccess) which fires visit_IndexAccess on the inner (visits grid, folds 3→2, sets inner.index_converted). Then the outer folds 2→1, sets outer.index_converted. Result: grid[1][2]. This is correct and natural — no special nested handling needed.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -m pytest tests/test_analyzer.py -k "A1_index or A2_index or Ax_nested or Ax_index" --tb=short -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - test_A1_index_literal_rewritten passes (items[1] → index.value==0, index_converted True)
    - test_A1_nested_index_rewritten passes (grid[2][3] → outer index 1, inner index 2)
    - test_A1_variable_index_uses_atena_index_helper passes (items[i] → FunctionCall(_atena_index))
    - test_A2_index_zero_error passes ("start at 1, not 0" in report)
    - test_A2_index_negative_error passes ("no negative positions" in report)
    - test_Ax_nested_subscript_independent passes (no double-shift)
    - test_Ax_index_converted_idempotent passes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted Atena source → BinOp/IndexAccess visitors | Arbitrary nesting of + chains and subscript chains processed by recursive _visit calls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of Service | visit_BinOp recursive chain (deeply nested "a"+"b"+"c"...+"z") | accept | Teaching programs are bounded. Python's recursion limit handles any realistic program. Phase-5 driver catches RecursionError. |
| T-03-03 | Tampering | _COERCE_TABLE lookup with unlisted (left_type, right_type) pairs | mitigate | Table uses dict.get(..., "error") — unlisted pairs fall to the "error" outcome (emits a plain-English message) rather than silently passing through. This prevents unknown type combinations from generating potentially-crashing Python. |
| T-03-04 | Tampering | IndexAccess.index_converted double-shift via re-analysis | mitigate | Idempotency guard (node.index_converted is True → skip rewrite) is the first check in visit_IndexAccess. Tests assert the guard fires (test_Ax_index_converted_idempotent). |
| T-03-SC | Tampering | No package installs in this plan | accept | Plan modifies only Python source files. No supply-chain surface. |
</threat_model>

<verification>
After all three tasks, run the full test suite to confirm GREEN for SEM-01..SEM-05 stubs:
  cd /Users/juliorcoelho/atena-lang && python -m pytest tests/test_analyzer.py -k "A1 or A2_index or A2_cannot or Ax_chain or Ax_nested or Ax_index" --tb=short -q

Expect: all SEM-01..SEM-05 covering stubs pass. SEM-06/07 stubs (A2_undefined, A2_call_before, A2_wrong_arity, A2_function_reads_outer, Ax_poison) remain RED.

Also verify the full suite still imports:
  python -m pytest tests/ --collect-only -q 2>&1 | tail -5
</verification>

<success_criteria>
- _COERCE_TABLE fully populated with all known-type pair outcomes
- _HUMAN_TYPE maps all six lattice members
- visit_BinOp injects str() FunctionCall for string+number/bool, _atena_concat for unknown side, errors for disallowed combos
- visit_IndexAccess folds literal positives, errors on literal-0 and literal-negative, routes dynamic to _atena_index helper; idempotent
- Literal visitor methods return correct type strings
- All A1_ and A2_ test stubs for SEM-01 through SEM-05 pass GREEN
- SEM-06/07 test stubs remain RED (no scope/arity logic yet)
</success_criteria>

<output>
Create .planning/phases/03-semantic-analyzer/03-02-SUMMARY.md when done.
</output>
