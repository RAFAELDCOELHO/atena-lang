---
phase: 02-parser
plan: 05
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - tests/test_parser.py
autonomous: true
requirements:
  - PARSE-01
  - PARSE-02
  - PARSE-03
  - PARSE-04
  - PARSE-05
  - PARSE-06

must_haves:
  truths:
    - "A program with nested blocks (if inside while inside function) parses to arbitrary depth"
    - "String: 'a or b and c' parses as or(a, and(b, c)) — operator precedence verified end-to-end"
    - "A mix of valid and invalid statements in one source file: all errors collected, all valid statements parsed"
    - "The parser produces the correct Program AST for the golden school.atena program structure (statement count, node types)"
  artifacts:
    - path: "tests/test_parser.py"
      provides: "Additional cross-requirement and golden integration tests added to Layer 3 section"
      contains: "test_Px_golden_program"
  key_links:
    - from: "tests/test_parser.py::test_Px_golden_program"
      to: "src/atena/parser.py::parse()"
      via: "_parse() helper chains lexer → parser; asserts statement count and node types"
      pattern: "_parse("
---

<objective>
Add golden end-to-end integration tests and cross-phase coverage checks that verify all 6 PARSE
requirements are satisfied together, not just in isolation.

Purpose: Plans 01–04 build the parser feature-by-feature. This plan adds the integration gate:
a golden multi-statement Atena program is parsed correctly, and the Looks-Done-But-Isn't checklist
from PITFALLS.md is covered by targeted tests.

Output: Additional Layer 3 tests appended to tests/test_parser.py. No src/ changes.
All tests must pass after Plan 04 is complete (this plan runs in wave 4 parallel with Plan 04,
but its tests validate the fully-wired parser).
</objective>

<execution_context>
@/Users/juliorcoelho/atena-lang/.planning/phases/02-parser/02-05-PLAN.md
@/Users/juliorcoelho/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/juliorcoelho/atena-lang/.planning/ROADMAP.md
@/Users/juliorcoelho/atena-lang/.planning/phases/02-parser/02-CONTEXT.md
@/Users/juliorcoelho/atena-lang/.planning/phases/02-parser/02-03-SUMMARY.md

<interfaces>
<!-- These tests should run AFTER Plan 04 is merged, so all 4 parser plans are complete.
     If run before Plan 04, some tests will be RED (expected). -->

<!-- The golden school.atena structure (from ARCHITECTURE.md / fixtures/):
     A full program with: function definition(s), variable assignments, show, ask, if/else,
     while, repeat, list/dict operations. The parser test does NOT need to match the exact source;
     it asserts the statement count and high-level node types from a representative snippet. -->

<!-- PITFALLS.md "Looks Done But Isn't" checklist items owned by the parser:
     - Parser error recovery: malformed input terminates; error count is bounded and deduplicated.
     - Unary vs binary minus: "-5", "5 - 3", "a - -b", "-a + b" all parse and evaluate correctly.
     - Postfix chaining: a[1][2], chain inside larger expression.
     Note: execution tests (run generated Python) are Phase 4's responsibility; parser tests assert AST shape only. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add golden integration tests and Looks-Done-But-Isn't coverage to tests/test_parser.py</name>
  <files>tests/test_parser.py</files>
  <read_first>
    - tests/test_parser.py — read the whole file to find the Layer 3 section and append after existing Px_ tests
    - src/atena/parser.py — understand what parse() returns for compound programs (verify after Plan 04 is applied)
    - .planning/research/PITFALLS.md — parser "Looks Done But Isn't" checklist (§parser section): unary/binary minus, postfix chaining, error-recovery no-hang, bounded error count
    - .planning/phases/02-parser/02-CONTEXT.md — phase success criteria from ROADMAP, Phase 2 done-definition
  </read_first>
  <behavior>
    Tests to add (all must pass after Plans 01–04 complete):

    test_Px_golden_program — Parse a representative multi-construct Atena program and assert shape:
      Source (multi-line, indented):
        function greet(name)\n
            show name\n
        \n
        score = 5\n
        greet("Ana")\n
        if score > 3\n
            show "pass"\n
        else\n
            show "fail"\n
        repeat 2 times\n
            add score to items\n
      Assert: program.statements has 5 top-level nodes (FunctionDef, Assign, FunctionCall, If, Repeat)
      Assert: isinstance(program.statements[0], FunctionDef)
      Assert: program.statements[0].name == "greet"
      Assert: isinstance(program.statements[1], Assign)
      Assert: isinstance(program.statements[3], If) and len(program.statements[3].else_body) == 1
      Assert: isinstance(program.statements[4], Repeat)
      Assert: ec.is_empty()

    test_Px_unary_minus_in_expression — "-a + b" → BinOp("+", UnaryOp("-", Identifier("a")), Identifier("b"))
      (distinct from "-(a + b)"; verifies unary binds tighter than binary per Pitfall 7):
      Source: "x = -a + b\n"
      Assert: assign.value is BinOp(op="+"); assign.value.left is UnaryOp(op="-"); ec.is_empty()

    test_Px_postfix_index_inside_expression — binary op applied to postfix chain:
      Source: "x = total + scores[1]\n"
      Assert: BinOp("+", Identifier("total"), IndexAccess(Identifier("scores"), NumberLiteral(1), False))
      Assert: ec.is_empty()

    test_Px_deep_nesting — function containing if containing while:
      Source:
        function process(x)\n
            if x > 0\n
                while x > 0\n
                    x = x - 1\n
      Assert: FunctionDef body contains If; If.then_body contains While; ec.is_empty()

    test_Px_error_count_bounded — ten bad statements produce at most 10 errors (not 100):
      Source: "def f()\n" * 10 (or 10 lines of bad syntax)
      ec.report() count("Error on line") should be <= 10
      Note: ErrorCollector.ERROR_CAP=10 is enforced at report() time; this test verifies the cap
      applies. The parser itself may collect > 10 but report() renders at most 10.

    test_Px_valid_after_errors — valid statement after two bad statements is still parsed:
      Source: "def f()\nfor i in items\nx = 5\n"
      Assert: not ec.is_empty() (2 errors collected)
      Assert: at least one Assign node exists in program.statements (x=5 was parsed after recovery)

    test_Px_comparison_precedence — comparison binds looser than arithmetic:
      Source: "x = a + b == c + d\n"
      Assert: top-level BinOp is "==" with left=BinOp("+", a, b) and right=BinOp("+", c, d)
      Assert: ec.is_empty()

    test_Px_logical_not_in_condition — not used in condition:
      Source: "if not x == 0\n    show x\n"
      Assert: If.condition is UnaryOp(op="not", operand=BinOp("==", Identifier("x"), NumberLiteral(0)))
      Assert: ec.is_empty()

    test_Px_return_inside_function_no_error (already in Layer 3 from Plan 01 — skip if duplicate):
      If not already present: "function f()\n    return 1\n" → ec.is_empty()
  </behavior>
  <action>
    Open tests/test_parser.py (read it first — do not overwrite, only APPEND new tests).

    Append the new Px_ tests to the end of the Layer 3 section (or create a new "Layer 3 Extension —
    Integration and Pitfall Coverage" subsection header comment).

    Each test:
    - Uses the _parse() helper already defined in test_parser.py.
    - Has a docstring stating what ROADMAP success criterion or PITFALLS pitfall it covers.
    - Makes specific, non-subjective assertions (isinstance, count, op value, .name field value).
    - Does NOT use pytest.mark.timeout or sleep — the no-infinite-loop property is validated by
      test_Px_malformed_no_infinite_loop from Plan 01.

    For test_Px_golden_program, indent the multi-line source with Python triple-quoted strings and
    actual \n line endings; do NOT use \t for the Atena indented lines — use 4 spaces.

    Do NOT modify any existing test — only add new tests at the end.

    Git commit: "test(02-05): add integration and pitfall-coverage tests for Phase 2 parser"
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -m pytest tests/test_parser.py --tb=short -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - All new Px_ integration tests pass (assuming Plan 04 is complete).
    - No existing test is broken.
    - test_Px_golden_program asserts 5 top-level statements with correct types.
    - test_Px_valid_after_errors confirms recovery: valid code after bad code is still parsed.
    - All 6 PARSE requirements have at least one test that exercises them end-to-end.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_parser.py → parser under test | integration tests feed multi-line programs; parser must handle them gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Availability | test_Px_error_count_bounded on pathological input | accept | ErrorCollector.ERROR_CAP=10 caps render output; the test verifies the cap; no hang risk since each "def f()" is one bad statement with deterministic recovery |
| T-02-SC | Tampering | No package installs in this plan | accept | This plan only modifies tests/test_parser.py (pure Python, no npm/pip/cargo); slopcheck not required |
</threat_model>

<verification>
After the plan completes (requires Plan 04 to be done):

1. pytest tests/test_parser.py exits 0 (all tests pass including new integration tests).
2. test_Px_golden_program: 5 top-level statements, correct types, ec.is_empty().
3. test_Px_valid_after_errors: error + valid → error collected AND valid node in program.statements.
4. test_Px_unary_minus_in_expression: BinOp("+", UnaryOp("-", ...), ...) — unary binds tighter.
5. No test hangs.

Run: cd /Users/juliorcoelho/atena-lang && python -m pytest tests/test_parser.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR" | tail -50
</verification>

<success_criteria>
- All new integration tests pass.
- Phase 2 is 100% green across all three test layers.
- The ROADMAP success criteria for Phase 2 are each covered by at least one test:
  1. PARSE-01/03: test_Px_golden_program (full Program AST with nested blocks).
  2. PARSE-02: test_Px_unary_minus_in_expression, test_Px_comparison_precedence, test_P1_left_associativity (Plan 01).
  3. PARSE-03: test_Px_deep_nesting (arbitrary nesting), test_P1_nested_blocks (Plan 01).
  4. PARSE-04: test_Px_golden_program (function, return, call, list, dict, index, dot, add/remove).
  5. PARSE-05: test_Px_three_bad_statements_three_errors (sync recovery).
  6. PARSE-06: test_Px_malformed_no_infinite_loop (no hang), all P2_ redirect tests.
- Committed on feat/parser branch.
</success_criteria>

<output>
Create `.planning/phases/02-parser/02-05-SUMMARY.md` when done.
</output>
