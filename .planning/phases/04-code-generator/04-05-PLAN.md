---
phase: 04-code-generator
plan: 05
type: execute
wave: 5
depends_on:
  - "04-04"
files_modified:
  - tests/fixtures/school.atena
  - tests/fixtures/school.expected.py
  - tests/fixtures/keyword_mangle.atena
  - tests/fixtures/keyword_mangle.expected.py
  - tests/fixtures/nested_repeat.atena
  - tests/fixtures/nested_repeat.expected.py
  - tests/fixtures/dynamic_index.atena
  - tests/fixtures/dynamic_index.expected.py
  - tests/fixtures/concat_helper.atena
  - tests/fixtures/concat_helper.expected.py
  - tests/fixtures/dict_dot_write.atena
  - tests/fixtures/dict_dot_write.expected.py
  - tests/test_codegen.py
  - examples/school.atena
autonomous: false
requirements:
  - GEN-06
  - GEN-01
  - GEN-02
  - GEN-03
  - GEN-04
  - GEN-05

must_haves:
  truths:
    - "school.atena runs through the full pipeline with zero errors"
    - "Generated Python for school.atena exactly matches school.expected.py byte-for-byte"
    - "Running school.expected.py with stdin 'Ana' produces the expected personalized output"
    - "test_G1_golden_school_roundtrip passes (text-match against school.expected.py)"
    - "test_G2_school_execution_with_canned_stdin passes (subprocess with input='Ana\\n')"
    - "All six targeted fixture pairs have passing text-match and execution tests"
    - "User reviewed and approved school.atena and school.expected.py before lock"
    - "examples/school.atena is identical to tests/fixtures/school.atena"
  artifacts:
    - path: "tests/fixtures/school.atena"
      provides: "Canonical capstone: I/O, variables, if/else, while, nested repeat, functions+return, lists, dicts (read+write), str() coercion, arithmetic"
      min_lines: 30
    - path: "tests/fixtures/school.expected.py"
      provides: "Derived snapshot: pipeline output reviewed and locked"
      contains: "# Generated by Atena"
    - path: "examples/school.atena"
      provides: "User-facing curriculum flagship (identical to tests/fixtures/school.atena)"
    - path: "tests/test_codegen.py"
      provides: "test_G1_golden_school_roundtrip, test_G2_school_execution_with_canned_stdin, targeted fixture battery"
  key_links:
    - from: "tests/test_codegen.py"
      to: "tests/fixtures/school.atena + school.expected.py"
      via: "test_G1_golden_school_roundtrip reads both files and asserts text equality"
      pattern: "school\\.atena"
    - from: "tests/fixtures/school.expected.py"
      to: "subprocess"
      via: "test_G2_school_execution_with_canned_stdin runs with input='Ana\\n'"
      pattern: "Ana"
---

<objective>
Author `school.atena` (canonical capstone), derive and lock `school.expected.py` (golden snapshot), implement the golden + execution tests, author five targeted fixture pairs, present school.atena and school.expected.py to the user for review before locking.

Purpose: GEN-06 is the integration acceptance gate for the entire Phase 4. The golden round-trip (text-match + execution test) confirms the full pipeline works end-to-end. Targeted fixtures confirm edge cases the capstone cannot isolate.

Output: All fixtures authored, golden snapshot locked after human review, all GEN-06 tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-code-generator/04-CONTEXT.md
@.planning/phases/04-code-generator/04-PATTERNS.md
@.planning/phases/04-code-generator/04-04-SUMMARY.md

<interfaces>
<!-- school.atena construct checklist (D-03/D-04/D-05 from 04-CONTEXT.md) -->

Required constructs in school.atena:
  - ask → input() [string only per D-05 — ask for student name, no number parsing]
  - variables (assign, use, show)
  - if / else (conditional on computed result)
  - while OR repeat (for summing/looping)
  - nested repeat (inner repeat inside outer loop)
  - function definitions + return
  - function calls
  - lists: literal creation, add, remove, length, literal index, variable index
  - dicts: literal creation, dot read, dot write (student.field = value)
  - str() coercion: "Student: " + name → triggers static coerce at analysis time
  - arithmetic (sum, average)
  - comparison (>= for pass/fail threshold)
  - canned stdin "Ana\n" → deterministic output that includes "Ana"

D-05 constraint: grades MUST be literal data in the program, NOT asked via `ask`.
D-06 snapshot process: derive school.expected.py by running _generate(school_src), not hand-authoring.

Targeted fixture shapes:
  keyword_mangle.atena:   `class = 5\nshow class\n`
  nested_repeat.atena:    nested repeat counting total iterations
  dynamic_index.atena:    `i = 1\ngrades = [5, 7, 9]\nshow grades[i]\n`
  concat_helper.atena:    function with unknown-typed params joined via `+`
  dict_dot_write.atena:   dict literal + dot write + dot read round-trip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author school.atena + all targeted fixtures, derive all expected.py snapshots</name>
  <files>tests/fixtures/school.atena, tests/fixtures/school.expected.py, tests/fixtures/keyword_mangle.atena, tests/fixtures/keyword_mangle.expected.py, tests/fixtures/nested_repeat.atena, tests/fixtures/nested_repeat.expected.py, tests/fixtures/dynamic_index.atena, tests/fixtures/dynamic_index.expected.py, tests/fixtures/concat_helper.atena, tests/fixtures/concat_helper.expected.py, tests/fixtures/dict_dot_write.atena, tests/fixtures/dict_dot_write.expected.py, examples/school.atena</files>
  <read_first>
    - .planning/phases/04-code-generator/04-CONTEXT.md (D-03 school/grades shape, D-04 full construct set, D-05 grades as literals, D-06 derived snapshot process)
    - src/atena/parser.py (verify dot-write fix from Plan 01 is present — look for _dot_target handling)
    - tests/test_codegen.py (test_G1_golden_school_roundtrip and test_G2_school_execution_with_canned_stdin stubs — understand what they expect)
    - .planning/phases/04-code-generator/04-PATTERNS.md (school.atena shape section, fixture directory structure, _generate() pipeline helper for derivation)
  </read_first>
  <action>
    Author and write all fixture files in this order:

    STEP 1 — Write `tests/fixtures/school.atena` and `examples/school.atena` (same content):
    Design a coherent school/grades program. Required construct checklist (all must appear):
    - One `ask` for student name (string) — feeds canned stdin "Ana"
    - Literal grades list: `grades = [8, 6, 9, 7]`
    - Dict literal for student record: `student = {name = "waiting", passed = false}`
    - A function `sum_list(nums)` that uses `while` or `repeat` to sum elements and `return`s the total
    - A function `average(nums)` that calls `sum_list` and divides by `length nums`, returns the result
    - Call `average(grades)` and store in a variable
    - `if`/`else` branch: if average >= 6 then `student.passed = true` and show pass message; else show fail
    - Update `student.name` via dot-write: `student.name = name`
    - Show personalized greeting using string concatenation: `show "Welcome, " + name`
    - Demonstrate list ops: `add` a bonus grade to grades, `remove` it, `show length grades`
    - Demonstrate literal index: `show grades[1]` (the first grade)
    - Demonstrate variable index: `i = 2\nshow grades[i]` (second grade via variable index)
    - Demonstrate dict dot read: `show student.name`
    - Include a nested `repeat` (e.g., repeat 2 times inside another repeat 2 times, showing count)
    Keep the program coherent — it should read as a real student grade checker, not a random feature demo.
    The narrative: ask name, check grades, compute average, determine pass/fail, show results.

    STEP 2 — Derive and write `tests/fixtures/school.expected.py`:
    Run the pipeline programmatically:
    ```python
    from atena.errors import ErrorCollector
    from atena.lexer import Lexer
    from atena.parser import Parser
    from atena.analyzer import SemanticAnalyzer
    from atena.codegen import CodeGenerator
    school_src = Path("tests/fixtures/school.atena").read_text()
    ec = ErrorCollector()
    toks = Lexer(school_src, ec).tokenize()
    prog = Parser(toks, ec).parse()
    SemanticAnalyzer(prog, ec).analyze()
    assert ec.is_empty(), f"school.atena errors: {ec.report()}"
    py_src = CodeGenerator(prog).generate()
    ```
    Verify it executes: `subprocess.run([sys.executable, "-c", py_src], input="Ana\n", ...)` → returncode 0, "Ana" in stdout.
    Write `py_src` to `tests/fixtures/school.expected.py`.

    STEP 3 — Write targeted fixtures (each is a minimal Atena program + derived expected.py):

    `tests/fixtures/keyword_mangle.atena`:
    Content: `class = 5\nshow class\n`
    Derive `tests/fixtures/keyword_mangle.expected.py` via pipeline. Assert `"class_"` in output.

    `tests/fixtures/nested_repeat.atena`:
    Content:
    ```
    count = 0
    repeat 3 times
        repeat 2 times
            count = count + 1
    show count
    ```
    Derive expected.py. Assert 2 distinct `_atena_i*` vars in output. Execute → stdout is `6`.

    `tests/fixtures/dynamic_index.atena`:
    Content:
    ```
    grades = [5, 7, 9]
    i = 1
    show grades[i]
    ```
    Derive expected.py. Assert `"_atena_index"` in output. Execute → stdout is `5`.

    `tests/fixtures/concat_helper.atena`:
    Content:
    ```
    function join(a, b)
        return a + b
    show join(3, 5)
    ```
    Derive expected.py. Assert `"_atena_concat"` in output. Execute → stdout is `35` (string concat).

    `tests/fixtures/dict_dot_write.atena`:
    Content:
    ```
    student = {name = "temp", grade = 0}
    student.name = "Ana"
    student.grade = 9
    show student.name
    show student.grade
    ```
    Derive expected.py. Assert subscript Store in output. Execute → stdout contains `Ana` then `9`.

    Restrictions:
    - Do NOT ask for numbers via `ask` — D-05: ask is string-only; grades must be literal data.
    - Do NOT use features outside Atena v1.0 grammar (no elif, no floats, no string escaping).
    - Write the expected.py files AFTER running and verifying each pipeline output executes correctly.
    - Use Write tool for all file creation. Do NOT use bash heredoc or cat commands.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -m pytest tests/ -q --tb=short -k "not golden and not school_execution" 2>&1 | tail -10</automated>
  </verify>
  <done>
    All six fixture pairs exist in tests/fixtures/. school.atena runs through pipeline with zero errors. school.expected.py matches _generate(school.atena) byte-for-byte. Executing school.expected.py with stdin "Ana" exits 0 and includes "Ana" in stdout. All targeted fixtures execute correctly. examples/school.atena exists with same content as tests/fixtures/school.atena. All non-golden tests pass (golden tests are wired in Task 3, after human review in Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Task 1 authored school.atena (the canonical capstone program) and derived school.expected.py (the golden snapshot). The pipeline has been verified to produce school.expected.py from school.atena, and the generated Python executes correctly with stdin "Ana".

    Files for review:
    - tests/fixtures/school.atena — the Atena source program (your teaching capstone)
    - tests/fixtures/school.expected.py — the generated Python (derived snapshot, to be locked)
    - examples/school.atena — same as tests/fixtures/school.atena (user-facing copy)
  </what-built>
  <how-to-verify>
    1. Read `tests/fixtures/school.atena` — verify it:
       - Asks for student name via `ask`
       - Uses grades as literal list data (not asked via `ask`)
       - Has functions with `return`, lists (add/remove/length/index), dicts (dot read + dot write)
       - Has `if`/`else`, `while` or `repeat`, nested `repeat`
       - Shows a personalized greeting using the asked name
       - Reads coherently as a teaching program a non-programmer could understand

    2. Read `tests/fixtures/school.expected.py` — verify it:
       - Starts with a `# Generated by Atena` header comment
       - Uses double quotes for string literals (not single quotes)
       - Has blank lines between top-level function definitions
       - Has correct Python constructs matching the Atena source
       - Looks like readable Python a student could learn from

    3. Run it yourself:
       ```
       python tests/fixtures/school.expected.py
       ```
       Enter `Ana` when prompted. Verify the output is sensible (greeting, grade average, pass/fail).

    4. Run the automated round-trip check:
       ```
       cd /Users/juliorcoelho/atena-lang && python -c "
       import sys, subprocess
       sys.path.insert(0, 'src')
       from atena.errors import ErrorCollector
       from atena.lexer import Lexer
       from atena.parser import Parser
       from atena.analyzer import SemanticAnalyzer
       from atena.codegen import CodeGenerator
       from pathlib import Path
       src = Path('tests/fixtures/school.atena').read_text()
       expected = Path('tests/fixtures/school.expected.py').read_text()
       ec = ErrorCollector()
       toks = Lexer(src, ec).tokenize()
       prog = Parser(toks, ec).parse()
       SemanticAnalyzer(prog, ec).analyze()
       assert ec.is_empty(), ec.report()
       out = CodeGenerator(prog).generate()
       assert out == expected, 'Mismatch!'
       r = subprocess.run([sys.executable, '-c', out], input='Ana\n', capture_output=True, text=True, timeout=10)
       assert r.returncode == 0, r.stderr
       assert 'Ana' in r.stdout, r.stdout
       print('PASS:', r.stdout.strip()[:100])
       "
       ```
  </how-to-verify>
  <resume-signal>
    Type "approved" to lock school.atena and school.expected.py as the golden fixtures and proceed to Task 3.
    Or describe any issues (wrong constructs, incoherent program, bad Python output) and the executor will:
    1. Re-author tests/fixtures/school.atena to address the issues you describe.
    2. Re-derive tests/fixtures/school.expected.py by re-running the pipeline derivation step from Task 1 (STEP 2) on the updated school.atena.
    3. Verify the updated expected.py executes with stdin "Ana" before re-presenting.
    4. Re-present both files for another round of review.
    Repeat until "approved".
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Implement golden + targeted fixture tests; final full-suite GREEN</name>
  <files>tests/test_codegen.py</files>
  <read_first>
    - tests/test_codegen.py (current state — G1_golden_school_roundtrip and G2_school_execution stubs from Plan 01, plus all other implemented tests)
    - tests/fixtures/school.atena (the approved capstone)
    - tests/fixtures/school.expected.py (the locked golden snapshot)
    - .planning/phases/04-code-generator/04-PATTERNS.md (Layer 1 golden test pattern, Layer 2 execution test pattern using subprocess)
  </read_first>
  <action>
    Implement all remaining test stubs to make the full test_codegen.py suite GREEN.

    Replace stub `test_G1_golden_school_roundtrip` with real implementation:
    - Read `tests/fixtures/school.atena` and `tests/fixtures/school.expected.py` via Path.
    - Call `_generate(source)`.
    - Assert `result == expected` with a diff-showing error message on mismatch.

    Replace stub `test_G2_school_execution_with_canned_stdin` with real implementation:
    - Read `tests/fixtures/school.expected.py`.
    - Run via `subprocess.run([sys.executable, "-c", python_src], input="Ana\n", capture_output=True, text=True, timeout=10)`.
    - Assert `returncode == 0`.
    - Assert `"Ana"` in `result.stdout`.
    - Assert the output contains a pass/fail verdict line (assert either "pass" or "fail" appears, case-insensitive, confirming the if/else branch ran).

    Add targeted fixture tests (one per pair):

    `test_G1_keyword_mangle_fixture`:
    - `_generate(Path("tests/fixtures/keyword_mangle.atena").read_text())` matches `keyword_mangle.expected.py`.
    - Assert `"class_"` in result.

    `test_G2_keyword_mangle_execution`:
    - Run `keyword_mangle.expected.py` via subprocess → stdout is `5`.

    `test_G1_nested_repeat_fixture`:
    - Text-match against `nested_repeat.expected.py`.
    - Assert 2 distinct `_atena_i*` loop vars.

    `test_G2_nested_repeat_execution`:
    - Run `nested_repeat.expected.py` → stdout is `6`.

    `test_G1_dynamic_index_fixture`:
    - Text-match against `dynamic_index.expected.py`.
    - Assert `"_atena_index"` in result.

    `test_G2_dynamic_index_execution`:
    - Run `dynamic_index.expected.py` → stdout is `5`.

    `test_G1_concat_helper_fixture`:
    - Text-match against `concat_helper.expected.py`.
    - Assert `"_atena_concat"` in result.

    `test_G2_concat_helper_execution`:
    - Run `concat_helper.expected.py` → stdout is `35` (string concatenation result of str(3)+str(5)).

    `test_G1_dict_dot_write_fixture`:
    - Text-match against `dict_dot_write.expected.py`.

    `test_G2_dict_dot_write_execution`:
    - Run `dict_dot_write.expected.py` → stdout contains `Ana` and `9`.

    Final gate: run the complete test suite to confirm all tests pass. If any test fails, fix the root cause (in codegen.py or the fixture) before marking done.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -m pytest tests/ -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>
    `pytest tests/` exits 0 with all tests passing (including all pre-existing phases 0–3 tests). test_G1_golden_school_roundtrip passes (byte-for-byte match). test_G2_school_execution_with_canned_stdin passes (subprocess with "Ana" stdin, output includes "Ana"). All 10 targeted fixture tests pass. test_G3_zero_error_gate, test_G3_ast_parse_selfcheck, and all Gx_* tests pass. GEN-01…GEN-06 requirements are fully satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| school.atena source → pipeline | The school.atena content is authored by Claude (trusted); it exercises all constructs but contains no adversarial input |
| Generated school.expected.py → subprocess execution | The golden snapshot is executed via subprocess for the execution test; it uses `input()` which consumes the canned "Ana\n" stdin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Information Disclosure | school.expected.py execution test uses subprocess with canned stdin | accept | Canned stdin is a fixed test string "Ana\n"; no real user data involved; subprocess is isolated from the test runner process |
| T-04-13 | Tampering | school.expected.py golden snapshot text-match | mitigate | Snapshot is derived from the pipeline (not hand-authored), reviewed once by the user (checkpoint), then locked; any future pipeline change causes the test to fail, surfacing the regression |
| T-04-14 | Tampering | examples/school.atena identical to tests/fixtures/school.atena | accept | Both files are authored by Claude and reviewed by user; the curriculum copy (examples/) is written identically to the fixture copy at authoring time; Phase 6 may update examples/ independently |
| T-04-SC | Tampering | No new package installs | accept | No npm/pip/cargo installs in Plan 05; stdlib subprocess only for tests |
</threat_model>

<verification>
1. `cd /Users/juliorcoelho/atena-lang && python -m pytest tests/ -q --tb=short` — exits 0, all tests pass.
2. `python -m pytest tests/test_codegen.py -v --tb=short` — shows test_G1_golden_school_roundtrip and test_G2_school_execution_with_canned_stdin both PASS.
3. Manual execution: `python tests/fixtures/school.expected.py` with input "Ana" → output contains "Ana" and a pass/fail verdict.
4. Diff check: `diff tests/fixtures/school.atena examples/school.atena` → no differences.
5. Requirement coverage: all of GEN-01, GEN-02, GEN-03, GEN-04, GEN-05, GEN-06 satisfied by at least one passing test.
</verification>

<success_criteria>
- GEN-06 satisfied: school.atena → school.expected.py text-match passes AND execution test with "Ana" stdin passes.
- All six targeted fixture pairs exist and have passing tests (text-match + execution).
- Full test suite (`pytest tests/`) exits 0.
- examples/school.atena exists and matches tests/fixtures/school.atena.
- Phase 4 is COMPLETE: GEN-01…GEN-06 all satisfied, no open regressions.
</success_criteria>

<output>
Create `.planning/phases/04-code-generator/04-05-SUMMARY.md` when done.
</output>
