---
phase: 06-packaging-curriculum
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "A new user can follow the README from install to running their first program without leaving the file"
    - "The README documents only the real CLI surface: `atena run`, `atena build`, `atena build --show` — no `--version`"
    - "The cheatsheet keyword list matches exactly the 19 keywords in `src/atena/tokens.py`"
    - "The README contains a 'When you make a mistake' error showcase (D-07) showing a plain-English Atena error with no Python traceback"
    - "The README contains a 'For teachers' section (D-09) describing the ~9-rung ladder as a classroom curriculum"
    - "The voice is warm and beginner-first throughout — 'you' not 'the user', no Python jargon (D-06)"
    - "The README opens with a 2-3 sentence 'what & why' hook before install (D-08)"
  artifacts:
    - path: "README.md"
      provides: "Getting-started guide for complete beginners"
      contains: "For teachers"
  key_links:
    - from: "README.md § Install"
      to: "pyproject.toml [project.scripts]"
      via: "`pip install .` exposes `atena` console script"
      pattern: "pip install"
    - from: "README.md § Language basics cheatsheet"
      to: "src/atena/tokens.py KEYWORDS"
      via: "19-keyword list must match tokens.py exactly"
      pattern: "show.*ask.*if.*else.*while.*repeat"
---

<objective>
Rewrite the one-line `README.md` stub into a full getting-started guide that takes a complete non-programmer from install to running their first program.

Purpose: DOCS-02 requires a README that explains installation, `atena run` / `atena build`, and the language basics. The README is also the product's face — it must showcase the core value (plain-English errors, no tracebacks) and position Atena as a classroom curriculum tool (D-09).

Output: A complete `README.md` with 8 sections (hook, install, first program, the two verbs, error showcase, language basics cheatsheet, examples pointer, for-teachers paragraph), written in a warm beginner-first voice with no Python jargon.
</objective>

<execution_context>
@/Users/juliorcoelho/atena-lang/.planning/phases/06-packaging-curriculum/06-03-PLAN.md
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/juliorcoelho/atena-lang/.planning/ROADMAP.md
@/Users/juliorcoelho/atena-lang/.planning/phases/06-packaging-curriculum/06-CONTEXT.md
@/Users/juliorcoelho/atena-lang/.planning/phases/06-packaging-curriculum/06-PATTERNS.md

<interfaces>
<!-- Key facts the executor needs — do not re-derive. -->

CLI surface (confirmed from src/atena/cli.py):
- `atena run file.atena` — transpiles and executes in memory; runtime errors → plain English Atena messages
- `atena build file.atena` — transpiles and writes `file.py`; prints `Built "file.py".`
- `atena build --show file.atena` — writes `file.py` AND prints generated Python 3 to stdout
- `atena` (no args) — prints help, exits 0
- NO `--version` flag exists — do NOT document it

19 keywords (from src/atena/tokens.py KEYWORDS — exact list, in this order):
show, ask, if, else, while, repeat, times, and, or, not, function, return, add, to, remove, from, length, true, false

Error voice examples (from src/atena/cli.py lines 82-110):
- `I couldn't find a file called "hello.atena".`
- `Error on line 2: I don't know what "xyz" is. Did you forget to create it?`
- The D-07 showcase should use the undefined-variable or zero-index error — both are beginner-relatable

9 example rungs (from 06-02-PLAN.md, D-13):
01-show.atena, 02-ask.atena, 03-variables.atena, 04-conditionals.atena,
05-while.atena, 06-repeat.atena, 07-functions.atena, 08-lists.atena, 09-dicts.atena
plus school.atena (capstone)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the full getting-started README</name>
  <files>README.md</files>

  <read_first>
    - `README.md` — current 1-line stub; read before overwriting
    - `src/atena/cli.py` — lines 32-50 (confirmed CLI verbs and flags) and lines 82-91 (error voice); read before documenting
    - `src/atena/tokens.py` — lines 97-117 (KEYWORDS dict); read to ensure the cheatsheet keyword list matches exactly
    - `examples/school.atena` — read to understand the capstone so the "first program" walkthrough or examples pointer is accurate
    - `.planning/phases/06-packaging-curriculum/06-CONTEXT.md` §D-05 through D-09 — the 8-section outline, voice rules, error showcase, for-teachers spec
    - `.planning/phases/06-packaging-curriculum/06-PATTERNS.md` §README.md — confirmed CLI facts, section outline, voice rule
  </read_first>

  <action>
Replace the entire contents of `README.md` with the full getting-started guide. Write it using the Write tool (do not use a heredoc Bash command). Follow this exact section structure and content specification:

SECTION 1 — Hook (D-08): H1 title "Atena", then 2-3 sentences:
  What Atena is (a teaching language that removes syntax noise and transpiles to Python 3).
  Who it is for (complete beginners, people who want to learn algorithmic logic without fighting syntax).
  The secret feature: `atena build --show` reveals the Python it generated, so learners see the real language they are growing toward.
  Keep this tight — motivate before mechanics, no wall of text.

SECTION 2 — Install (D-11): H2 "Install"
  One command: `pip install .` (run from the repo root).
  One sentence on what it does (places the `atena` command on your PATH).
  Contributor note: `pip install -e .` for an editable install.
  Do NOT mention PyPI — repo-install only.
  Do NOT mention `--version` — it does not exist.

SECTION 3 — First program (D-05 "install-to-first-program"): H2 "Write your first program"
  Show the learner how to create a file `hello.atena` containing:
    show "Hello, world!"
    show "Today's count: " + 5
  Then run it: `atena run hello.atena`
  Show the expected output:
    Hello, world!
    Today's count: 5
  Briefly note that the second line works because Atena handles the string+number conversion automatically.

SECTION 4 — The two verbs (D-05): H2 "Running and building programs"
  Three subsections or a compact table:
  - `atena run file.atena` — transpiles and runs the program immediately
  - `atena build file.atena` — translates to Python and saves `file.py`
  - `atena build --show file.atena` — also prints the generated Python to the screen
  Explain the `--show` flag in one sentence: it is how you peek at the Python that Atena wrote for you.
  Do NOT document `--version`.

SECTION 5 — Error showcase (D-07): H2 "When you make a mistake"
  Show a tiny buggy program, e.g.:
    score = 90
    show "Result: " + result
  (undefined variable `result` — relatable beginner mistake)
  Then the exact Atena error output (simulate what `atena run` would print):
    Error on line 2: I don't know what "result" is. Did you forget to create it?
      → show "Result: " + result
  One sentence: this is Atena's safety net — it always tells you the line, shows the offending code, and never dumps a wall of Python internals.

SECTION 6 — Language basics cheatsheet (D-05): H2 "Language basics"
  Subsections or a table covering these topics IN THIS ORDER:
  a) Output: `show "text"` / `show number` / `show "label: " + variable`
  b) Input: `name = ask "Enter your name: "` (always gives you text)
  c) Variables: `score = 10` / `greeting = "Hello"`
  d) Arithmetic: `+` `-` `*` `/` (integers only; automatic string coercion for `+` with text)
  e) Comparisons: `==` `!=` `>` `<` `>=` `<=`
  f) if / else:
       `if score >= 6`
           `show "Passing"`
       `else`
           `show "Not yet"`
  g) while loop:
       `i = 1`
       `while i <= 5`
           `i = i + 1`
  h) repeat loop:
       `repeat 3 times`
           `show "again"`
  i) Functions:
       `function double(n)`
           `return n * 2`
       `show double(5)`
  j) Lists (1-indexed!):
       `grades = [8, 9, 7]`
       `show grades[1]`  (first item — Atena counts from 1, not 0)
       `add 10 to grades`
       `remove 8 from grades`
       `show length(grades)`
  k) Dicts (dot notation):
       `person = {name = "Ana", age = 20}`
       `show person.name`
       `person.age = 21`
  l) Boolean values: `true` / `false`
  m) Logic operators: `and` / `or` / `not`

  AFTER THE CHEATSHEET: one small note on what Atena does NOT support in v1.0:
  "Atena v1.0 uses double-quoted strings and integers only. No floats, no string escaping, no elif."

SECTION 7 — Examples pointer (D-05): H2 "Examples"
  One paragraph: the `examples/` folder contains a 9-step concept ladder:
  List the 9 rungs by filename and concept (01-show.atena: output, …, 09-dicts.atena: dicts).
  Then `school.atena` is the capstone that combines everything.
  One sentence on how to run any of them: `atena run examples/01-show.atena`.

SECTION 8 — For teachers (D-09): H2 "For teachers"
  One paragraph (3-5 sentences):
  The 9-rung ladder is designed as a classroom curriculum — one rung ≈ one ~50-minute class.
  Each file is self-contained and self-explaining (the `#` comments inside narrate the concept step by step).
  A student who completes all 9 rungs has the foundation to write the capstone `school.atena` on their own.
  Atena's plain-English errors mean students see encouraging guidance rather than Python stack traces.
  The generated Python (`atena build --show`) gives motivated students a bridge to the real language.

VOICE RULES (D-06) — enforce throughout the entire document:
  - Use "you" not "the user" or "one"
  - No Python jargon: no "token", "AST", "traceback", "exception", "subprocess"
  - Match the encouraging voice of the error messages ("I couldn't find…")
  - Short sentences. Active voice.
  - Explain jargon if unavoidable (e.g. "transpiles — converts to Python 3")

Do NOT document `--version`. Do NOT claim Atena supports elif, floats, or string escaping.
  </action>

  <verify>
    <automated>
      cd /Users/juliorcoelho/atena-lang && python -c "
content = open('README.md').read()
assert '# Atena' in content or '# atena' in content.lower(), 'Missing H1 title'
assert 'pip install' in content, 'Missing install section'
assert 'atena run' in content, 'Missing run verb'
assert 'atena build' in content, 'Missing build verb'
assert '--show' in content, 'Missing --show flag'
assert '--version' not in content, 'Documented --version which does not exist'
assert 'For teachers' in content, 'Missing For teachers section (D-09)'
assert 'When you make a mistake' in content or 'make a mistake' in content, 'Missing error showcase (D-07)'
assert 'grades[1]' in content or 'items[1]' in content or '[1]' in content, 'Missing 1-indexing example in cheatsheet'
assert 'show' in content and 'ask' in content and 'if' in content and 'while' in content and 'repeat' in content and 'function' in content, 'Cheatsheet missing keywords'
assert 'str(' not in content, 'README must not use str() — Python jargon'
print('README.md checks PASSED')
"
    </automated>
  </verify>

  <acceptance_criteria>
    - `README.md` is longer than 100 lines (substantive content, not a stub)
    - File contains all 8 required sections: hook, install, first program, the two verbs, error showcase, language basics cheatsheet, examples pointer, for teachers
    - "For teachers" section present (D-09)
    - "When you make a mistake" (or equivalent heading) section present (D-07)
    - `atena run`, `atena build`, and `atena build --show` are documented
    - `--version` is NOT mentioned anywhere in the file
    - Cheatsheet covers: show, ask, if/else, while, repeat, function/return, lists with 1-indexing, dicts with dot access, automatic string coercion
    - The 19 keywords mentioned in the cheatsheet match exactly the KEYWORDS dict in `src/atena/tokens.py` — no extras, no omissions
    - No Python jargon in the body text ("traceback", "exception", "AST", "token", "subprocess" do not appear as concepts explained to the reader)
    - `str(` does not appear anywhere in the README (no Python-jargon-in-source violation)
    - The error showcase shows a plain-English Atena error (not a Python traceback)
    - Voice is second-person ("you") throughout — no "the user" or "one"
  </acceptance_criteria>

  <done>README.md is a complete getting-started guide. A brand-new user can follow it from `pip install .` to running their first Atena program without needing any other resource. The error showcase, cheatsheet, examples pointer, and for-teachers section are all present and accurate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md content | Documentation only — no code execution, no user input processing, no network I/O |
| CLI surface documented | README claims about CLI behavior must match the actual cli.py implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | README error showcase | accept | The error showcase intentionally reveals Atena's error format — this is the product's core value, not a leak. No secrets, no PII, no credentials in the example. |
| T-06-07 | Tampering | README CLI documentation | mitigate | Acceptance criteria requires `--version` is NOT documented (it does not exist); automated verify checks this. Documenting nonexistent flags would mislead users and break trust. |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan. README editing uses only the Write tool. |

No HIGH-severity threats present. README is a static documentation file with no runtime security surface. The only risk is incorrect documentation, which is mitigated by explicit acceptance criteria checks.
</threat_model>

<verification>
After the task completes:

1. `wc -l README.md` returns more than 100 lines
2. `grep -c "For teachers" README.md` returns 1
3. `grep -c "\-\-version" README.md` returns 0
4. `grep -c "\-\-show" README.md` returns at least 1
5. `grep "str(" README.md` returns nothing (no Python jargon)
6. The automated verify script in Task 1 exits 0
</verification>

<success_criteria>
DOCS-02 satisfied: the README is a complete getting-started guide covering installation, `atena run` / `atena build` / `atena build --show`, a first-program walkthrough, language basics cheatsheet, error showcase, examples pointer, and a for-teachers curriculum section — all in a warm beginner-first voice that matches Atena's plain-English error voice.
</success_criteria>

<output>
Create `.planning/phases/06-packaging-curriculum/06-03-SUMMARY.md` when done.
</output>
