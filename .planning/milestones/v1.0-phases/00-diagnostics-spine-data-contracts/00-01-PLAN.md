---
phase: "00"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/atena/__init__.py
  - src/atena/__main__.py
  - src/atena/errors.py
  - src/atena/tokens.py
  - src/atena/ast_nodes.py
  - src/atena/cli.py
  - src/atena/pipeline.py
  - tests/__init__.py
  - tests/conftest.py
autonomous: true
requirements:
  - DIAG-01
  - DIAG-02
  - DIAG-03
must_haves:
  truths:
    - "Running `pytest` from the repo root discovers and runs tests in `tests/` without ImportError"
    - "`import atena` succeeds in a venv where `pip install -e .` has been run"
    - "All seven `src/atena/` modules exist and are importable; `tokens.py` and `ast_nodes.py` import without depending on any sibling module"
    - "`python -m atena --help` executes (even if just exits) without a Python traceback"
  artifacts:
    - path: "pyproject.toml"
      provides: "Package metadata, hatchling build backend, pytest config, zero runtime deps"
      contains: "requires-python = \">=3.11\""
    - path: "src/atena/__init__.py"
      provides: "Package identity"
    - path: "src/atena/errors.py"
      provides: "Stub module — ErrorCollector skeleton (empty body) for importability; filled in Plan 02"
    - path: "src/atena/tokens.py"
      provides: "Stub module — importable; filled in Plan 03"
    - path: "src/atena/ast_nodes.py"
      provides: "Stub module — importable; filled in Plan 03"
    - path: "src/atena/cli.py"
      provides: "Stub module — importable; filled in Plan 05"
    - path: "src/atena/pipeline.py"
      provides: "Stub module — importable; filled in Plan 05"
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures (empty initially; populated per plan)"
  key_links:
    - from: "pyproject.toml"
      to: "src/atena/"
      via: "hatch.build.targets.wheel packages = [\"src/atena\"]"
      pattern: "packages.*src/atena"
    - from: "pyproject.toml"
      to: "tests/"
      via: "tool.pytest.ini_options testpaths = [\"tests\"]"
      pattern: "testpaths.*tests"
---

<objective>
Bootstrap the project skeleton: `pyproject.toml`, the `src/atena/` package layout, stub modules for every phase-0 module, and a `tests/` tree with a working pytest config. This plan creates NO logic — only the scaffolding that makes every subsequent plan's imports and test runs succeed from day one.

Purpose: Every later plan assumes `pip install -e .` works and `pytest` discovers tests. This plan makes that true.
Output: Importable `src/atena/` package with seven stub modules; `tests/` tree; `pyproject.toml` with hatchling backend and pytest config; zero runtime dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/juliorcoelho/atena-lang/.planning/PROJECT.md
@/Users/juliorcoelho/atena-lang/.planning/ROADMAP.md
@/Users/juliorcoelho/atena-lang/.planning/phases/00-diagnostics-spine-data-contracts/00-CONTEXT.md

<interfaces>
<!-- No prior code exists. This plan creates the foundation contracts others implement. -->
<!-- After this plan completes, every subsequent plan can assume: -->
<!--   from atena.errors import ErrorCollector   (stub exists) -->
<!--   from atena.tokens import TokenType, Token  (stub exists) -->
<!--   from atena.ast_nodes import Program, ...   (stub exists) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create `pyproject.toml` and src layout</name>
  <files>pyproject.toml, src/atena/__init__.py, src/atena/__main__.py, tests/__init__.py, tests/conftest.py</files>
  <read_first>
    - /Users/juliorcoelho/atena-lang/CLAUDE.md — project constraints (Python 3.11+, stdlib-only, hatchling, src layout, pytest 9.1.0)
    - /Users/juliorcoelho/atena-lang/.planning/research/STACK.md — the exact `pyproject.toml` template (Installation section) to follow verbatim
  </read_first>
  <action>
    Create `pyproject.toml` at the repo root with: `[build-system]` using `requires = ["hatchling"]` and `build-backend = "hatchling.build"`; `[project]` with `name = "atena-lang"`, `version = "0.1.0"`, `description = "Atena: a teaching language that transpiles to Python 3"`, `requires-python = ">=3.11"`, `dependencies = []`; `[project.scripts]` with `atena = "atena.cli:main"`; `[tool.hatch.build.targets.wheel]` with `packages = ["src/atena"]`; `[tool.pytest.ini_options]` with `testpaths = ["tests"]` and `addopts = "-v"`.

    Create `src/atena/__init__.py` as an empty file (just a module marker — no exports yet).

    Create `src/atena/__main__.py` containing a single call to `from atena.cli import main; main()` so `python -m atena` delegates to the CLI.

    Create `tests/__init__.py` as an empty file.

    Create `tests/conftest.py` with a module-level docstring noting it holds shared pytest fixtures; leave the body empty for now (future plans populate it).

    Do NOT add any `ruff`, `tox`, or linting config — STACK.md lists them as optional and this is day-one scaffolding.
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && pip install -e . --quiet 2>&1 | tail -5 && python -c "import atena; print('import ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `requires-python = ">=3.11"` and `dependencies = []`
    - `pyproject.toml` contains `atena = "atena.cli:main"` under `[project.scripts]`
    - `pyproject.toml` contains `testpaths = ["tests"]` under `[tool.pytest.ini_options]`
    - `python -c "import atena"` exits 0 in the venv after `pip install -e .`
    - `python -m atena --help` exits 0 or exits non-zero with a friendly message (not a Python traceback), because `cli.py` stub exists
  </acceptance_criteria>
  <done>pyproject.toml exists and is valid; `pip install -e .` succeeds; `import atena` works; tests/ tree exists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create stub modules for all phase-0 components</name>
  <files>src/atena/errors.py, src/atena/tokens.py, src/atena/ast_nodes.py, src/atena/cli.py, src/atena/pipeline.py</files>
  <read_first>
    - /Users/juliorcoelho/atena-lang/.planning/research/ARCHITECTURE.md — recommended project structure (§"Recommended Project Structure") and the list of modules; the `ErrorCollector` interface (`add`, `is_empty`, `report`); Token fields (`type`, `value`, `line`, `col`, `source_line`); full AST node-set
    - /Users/juliorcoelho/atena-lang/.planning/phases/00-diagnostics-spine-data-contracts/00-CONTEXT.md — D-10, D-11 (stub CLI scope, Phase 0 boundary)
  </read_first>
  <action>
    Create stubs for five modules. Each stub must be importable with NO errors and declare the names that later plans will implement. Do NOT add logic — only skeleton class/function signatures with `pass` or `...` bodies and a `# TODO: implemented in Plan 0X` comment.

    `src/atena/errors.py`: Define class `ErrorCollector` with three stub methods: `def add(self, line: int, message: str, source_line: str) -> None: ...`, `def is_empty(self) -> bool: ...`, `def report(self) -> str: ...`. Also define a stub `def suggest(name: str, candidates: list[str]) -> str | None: ...` at module level. Import only `from __future__ import annotations` — no sibling imports.

    `src/atena/tokens.py`: Define `class TokenType` (will be an enum in Plan 03) as a stub class with `pass`; define a stub `@dataclass class Token` with fields `type: object`, `value: str`, `line: int`, `col: int`, `source_line: str`. Import `from __future__ import annotations` and `from dataclasses import dataclass` only.

    `src/atena/ast_nodes.py`: Define a stub `@dataclass class Node` with field `line: int`; define one representative stub node `@dataclass class Program(Node)` with `statements: list` field. Import `from __future__ import annotations` and `from dataclasses import dataclass, field` only.

    `src/atena/pipeline.py`: Define a stub `def transpile(source: str, filename: str) -> str | None: ...` with a `# Phase 5: wired in pipeline integration phase` comment per D-11.

    `src/atena/cli.py`: Define a stub `def main() -> None: ...` (completely empty body for now). Import `import argparse` only.

    All five files: no sibling-module imports in stubs (tokens.py and ast_nodes.py must have zero non-stdlib imports per ARCHITECTURE.md "pure data, dependency-free" contract).
  </action>
  <verify>
    <automated>cd /Users/juliorcoelho/atena-lang && python -c "from atena.errors import ErrorCollector; from atena.tokens import Token; from atena.ast_nodes import Program; from atena.cli import main; from atena.pipeline import transpile; print('all stubs importable')"</automated>
  </verify>
  <acceptance_criteria>
    - `from atena.errors import ErrorCollector` succeeds with no ImportError
    - `from atena.tokens import Token` succeeds with no ImportError
    - `from atena.ast_nodes import Program, Node` succeeds with no ImportError
    - `from atena.cli import main` succeeds with no ImportError
    - `from atena.pipeline import transpile` succeeds with no ImportError
    - `python -c "import atena.tokens; import atena.ast_nodes"` succeeds with NO import of any sibling atena module (verify by reading the file — no `from atena.*` lines)
    - `pytest tests/ --collect-only` exits 0 (no collection errors even with empty test directory)
  </acceptance_criteria>
  <done>All five stub modules importable; `pytest --collect-only` exits 0; no sibling imports in tokens.py or ast_nodes.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root → Python environment | `pip install -e .` runs hatchling build backend; hatchling is a dev-time dep, not runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01-01 | Tampering | `pyproject.toml` `[project.scripts]` entry point | accept | Entry point is developer-controlled; no user-facing attack surface in this skeleton plan |
| T-00-01-02 | Information Disclosure | `pip install -e .` output on failure | accept | Failure messages are build-tool output to developer, not learner-visible |
| T-00-01-SC | Tampering | pip install of pytest / hatchling (dev deps) | mitigate | Both are well-known, widely-used packages; verify via `pip show pytest` after install; no `[ASSUMED]` or `[SUS]` packages in use |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/juliorcoelho/atena-lang
pip install -e . --quiet
python -c "from atena.errors import ErrorCollector; from atena.tokens import Token; from atena.ast_nodes import Program; print('OK')"
pytest tests/ --collect-only -q
```

All commands exit 0.
</verification>

<success_criteria>
- `pyproject.toml` exists with `requires-python = ">=3.11"`, zero `dependencies`, correct `[project.scripts]`, and `testpaths = ["tests"]`
- `pip install -e .` installs the package without errors
- All seven `src/atena/` modules importable individually
- `tokens.py` and `ast_nodes.py` have zero non-stdlib imports (grep confirms: `grep -v "^#\|^from __future__\|^from dataclasses\|^import" src/atena/tokens.py src/atena/ast_nodes.py | grep -c "^from\|^import"` equals 0)
- `pytest tests/ --collect-only` exits 0
</success_criteria>

<output>
Create `.planning/phases/00-diagnostics-spine-data-contracts/00-01-SUMMARY.md` when done
</output>
