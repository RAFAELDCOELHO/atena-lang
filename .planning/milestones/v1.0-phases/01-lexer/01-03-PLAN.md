---
phase: 01-lexer
plan: 03
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - src/atena/lexer.py
autonomous: true
requirements:
  - LEX-02
  - LEX-03
  - LEX-04

must_haves:
  truths:
    - "Lexing a nested-block program produces a balanced INDENT/DEDENT token stream (count of INDENT == count of DEDENT)"
    - "A file ending mid-block without a trailing newline drains all open blocks via DEDENT at EOF"
    - "Blank lines inside a block emit no tokens; the indent stack is untouched by blank lines"
    - "Comment-only lines (and deeply-indented comments) emit no tokens and do not change the parse"
    - "A staircase-dedent (indent 4, then 8, then back to 6) reports 'doesn't match' error"
    - "Mixed tabs and spaces in indentation reports 'tabs and spaces' error"
    - "Over-indent (jump of more than one unit) reports 'too far' error"
    - "Ragged-width indent (not a clean unit multiple) reports 'same size' error"
    - "After any indentation error, scanning continues and more errors can be collected (D-04)"
    - "After all 28 tests, python3 -m pytest tests/test_lexer.py -q exits 0 (all GREEN)"
  artifacts:
    - path: "src/atena/lexer.py"
      provides: "Complete Lexer — indentation engine (stack + uniform-step + EOF drain) + blank/comment skip integrated with core scanner"
      contains: "class Lexer"
  key_links:
    - from: "src/atena/lexer.py"
      to: "src/atena/errors.py"
      via: "self._errors.add(self._line, ...) for indentation errors"
      pattern: "doesn.t match|tabs and spaces|too far|same size"
    - from: "src/atena/lexer.py"
      to: "src/atena/tokens.py"
      via: "TokenType.INDENT, TokenType.DEDENT, TokenType.NEWLINE emitted by indentation handler"
      pattern: "TokenType\\.INDENT|TokenType\\.DEDENT|TokenType\\.NEWLINE"
---

<objective>
Integrate the indentation engine into src/atena/lexer.py: the INDENT/DEDENT/NEWLINE
emission layer (standard CPython stack algorithm), the uniform-step validation layer (D-05–D-07),
blank/comment-line skipping before the stack is touched (Pattern 3), and the EOF drain
(Pitfall 2). After this plan, all 28 test_lexer.py tests are GREEN and the phase gate passes.

Purpose: Complete the off-side-rule contract (LEX-02, LEX-03, LEX-04). The core scanner
(Plan 02) is preserved unchanged; this plan restructures the outer scan loop and adds the
_handle_indentation() and _drain_at_eof() methods.

Output: A complete src/atena/lexer.py; 28/28 tests GREEN; full suite (58 Phase 0 + 28) green.
</objective>

<execution_context>
@/Users/juliorcoelho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/juliorcoelho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-lexer/01-CONTEXT.md
@.planning/phases/01-lexer/01-RESEARCH.md
@.planning/phases/01-lexer/01-PATTERNS.md
@.planning/phases/01-lexer/01-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 02's output — do NOT redefine. Extend only. -->

From src/atena/lexer.py (Plan 02 deliverable):
  class Lexer:
    __init__(self, source: str, errors: ErrorCollector) -> None
      self._source, self._errors, self._lines, self._pos, self._line, self._col
      self._indent_stack: list[int] = [0]
      self._indent_char: str | None = None
      self._indent_unit: int | None = None
      self._tokens: list[Token] = []

    tokenize(self) -> list[Token]   ← outer loop; restructure this
    _scan_line(self, raw_line: str) -> None   ← inner scan; KEEP AS-IS
    _current(self) -> str | None
    _peek(self) -> str | None
    _advance(self) -> str | None
    _emit_token(self, tok_type, value, col, source_line) -> None

New methods to add (Plan 03):
    _handle_indentation(self, width: int, source_line: str) -> None
    _drain_at_eof(self) -> None
    _measure_indent(self, raw_line: str) -> tuple[int, str]  (returns width, indent_chars)

From src/atena/tokens.py:
  TokenType.INDENT, TokenType.DEDENT, TokenType.NEWLINE — structural tokens emitted here for the first time.

From src/atena/errors.py:
  ErrorCollector.add(line: int, message: str, source_line: str) -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add indentation engine, blank/comment skip, and EOF drain to src/atena/lexer.py</name>
  <files>src/atena/lexer.py</files>

  <read_first>
    - src/atena/lexer.py (Plan 02 output — the complete file to extend)
    - src/atena/tokens.py (TokenType.INDENT, DEDENT, NEWLINE — first use here)
    - src/atena/errors.py (ErrorCollector.add() — indentation error messages go here)
    - .planning/phases/01-lexer/01-RESEARCH.md §"Pattern 1: Standard Indentation Stack", §"Pattern 2: Uniform-Step Validation Layer", §"Pattern 3: Skip Blank and Comment-Only Lines", §"Pattern 7: EOF Drain", §"Pitfall 1: Blank/Comment Lines Touch the Indent Stack", §"Pitfall 2: EOF Drain Missing", §"Pitfall 3: Unmatched Dedent Not Detected", §"Pitfall 4: Uniform-Step Over-Indent Not Caught"
    - .planning/phases/01-lexer/01-CONTEXT.md §decisions D-05, D-06, D-07, D-08
    - tests/test_lexer.py (the 10 structural tests that must turn GREEN — read each test's assertion before implementing its case)
  </read_first>

  <behavior>
    Restructure tokenize() and _scan_line() to integrate the indentation engine. Keep the
    per-character scanner from Plan 02 unchanged — only the outer loop and line-start logic change.

    NEW METHOD: _measure_indent(self, raw_line: str) -> tuple[int, str]:
      Count leading whitespace. Return (width, indent_chars) where:
      - width = number of leading whitespace characters (raw count, not tab-expanded — Atena uses char-count, not column-count, so a tab counts as 1)
      - indent_chars = the leading whitespace substring (raw_line[:width])
      Example: "    show x\n" → (4, "    "); "\tshow x\n" → (1, "\t").
      Implementation: i = 0; while i < len(raw_line) and raw_line[i] in (' ', '\t'): i += 1; return i, raw_line[:i]

    NEW METHOD: _handle_indentation(self, width: int, source_line: str) -> None:
      Implements ARCHITECTURE Pattern 1 (standard stack) + CONTEXT D-05/D-06/D-07 (uniform-step layer).

      Standard stack algorithm (D-08 — do not deviate):
        top = self._indent_stack[-1]
        if width == top: return immediately (no tokens, no validation needed at same level)
        if width > top:
          INDENT branch:
            1. Tab/space mixing check (run BEFORE uniform-step unit check):
               If self._indent_char is not None:
                 existing_char = self._indent_char
                 If any character in the leading whitespace of this line is NOT existing_char:
                   self._errors.add(self._line, "Don't mix tabs and spaces for indentation — pick one and use it everywhere.", source_line)
                   (still push and emit INDENT so stream stays parseable per D-04)
               else:
                 Pin self._indent_char = leading[0] (first char of the leading whitespace).
                 Note: "leading whitespace" here = the width chars at the start of the raw line; pass it in as a separate arg or re-derive from self._lines[self._line - 1]. Keep _handle_indentation's signature as (self, width: int, indent_chars: str, source_line: str) — adding indent_chars makes the check clean without re-reading the source.
            2. Uniform-step unit check (only runs if no mixing error on this line):
               delta = width - top
               If self._indent_unit is None:
                 Pin self._indent_unit = delta (first indent sets the unit, D-06). No error.
               Else:
                 if delta > self._indent_unit:
                   self._errors.add(self._line, "This line is indented too far — keep each step the same size.", source_line)
                 elif delta != self._indent_unit:
                   self._errors.add(self._line, "Keep your indentation the same size as the rest of the file.", source_line)
                 (both errors still push to keep stream parseable — D-04 recover-and-continue)
            3. Push and emit: self._indent_stack.append(width); _emit_token(TokenType.INDENT, "", 0, source_line)
        if width < top:
          DEDENT branch:
            While self._indent_stack[-1] > width:
              self._indent_stack.pop()
              _emit_token(TokenType.DEDENT, "", 0, source_line)
            After the pop loop, check invariant:
              if self._indent_stack[-1] != width:
                self._errors.add(self._line, "This line's indentation doesn't match any block above it.", source_line)
                (Do NOT push or pop further — the error is reported, scanning continues)
            Also validate dedent width against uniform-step unit (only when stack DID match):
              if self._indent_stack[-1] == width and self._indent_unit is not None:
                if width != 0 and (width % self._indent_unit != 0):
                  self._errors.add(self._line, "Keep your indentation the same size as the rest of the file.", source_line)

      IMPORTANT: _handle_indentation signature must be (self, width: int, indent_chars: str, source_line: str) -> None so the tab/space mixing check has access to the raw leading characters.

    NEW METHOD: _drain_at_eof(self) -> None:
      At EOF, emit a trailing NEWLINE if the last emitted token was not a NEWLINE/DEDENT/INDENT:
        last = self._tokens[-1] if self._tokens else None
        if last is not None and last.type not in (TokenType.NEWLINE, TokenType.DEDENT, TokenType.INDENT, TokenType.EOF):
          _emit_token(TokenType.NEWLINE, "", 0, "")
      Drain all open blocks:
        while len(self._indent_stack) > 1:
          self._indent_stack.pop()
          _emit_token(TokenType.DEDENT, "", 0, "")
      Emit EOF:
        _emit_token(TokenType.EOF, "", 0, "")

    RESTRUCTURE tokenize():
      Replace the simple outer loop with:
        for raw_line in self._lines:
          source_line = raw_line.rstrip('\n')
          stripped = raw_line.lstrip()
          # Skip blank lines and comment-only lines BEFORE touching indent stack (Pattern 3)
          if not stripped or stripped.startswith('#'):
            self._line += 1   ← advance line counter even for skipped lines
            Note: self._line is managed by _advance() which increments on '\n'. Since skipped
            lines are not scanned by _advance(), manually increment here to keep line numbers
            accurate. However, because self._lines includes '\n' via keepends=True and the
            PREVIOUS call to _scan_line consumed each '\n', self._line is already correct.
            Actually: the _advance() method increments self._line when it consumes '\n'. Since
            blank/comment lines are skipped without calling _scan_line (and hence without
            consuming their '\n' via _advance), we need a different approach:
            CORRECT APPROACH: do NOT iterate over self._lines. Instead, iterate character-by-
            character using the global pos cursor (self._pos). At each line start, peek ahead
            to detect blank/comment and either skip (advance pos past the '\n' without processing)
            or process normally. This keeps self._line consistent.
            SIMPLER: keep the line-by-line loop, but track self._line explicitly from the loop
            index rather than from _advance(). Set self._line = line_index + 1 at the top of each
            iteration. _advance() increments self._col but does NOT increment self._line (remove
            that responsibility from _advance). Instead, self._line is updated by the outer loop.
            This avoids the double-increment problem.

            FINAL DESIGN (simplest and most robust):
            Iterate over enumerate(self._lines) to get (line_index, raw_line).
            At the start of each iteration: set self._line = line_index + 1 (1-based).
            Blank/comment skip: if not raw_line.strip() or raw_line.lstrip().startswith('#'): continue (skip entirely).
            Otherwise: measure indent, call _handle_indentation, emit NEWLINE at end of line, call the per-char scanner.
            Because self._line is set by the loop, _advance() must NOT increment self._line — remove that side-effect from _advance(); _advance() only updates self._pos and self._col.
            self._col is reset to 0 at the top of each line (before calling the scanner).

        After the loop: call self._drain_at_eof().
        Return self._tokens.

    RESTRUCTURE _scan_line():
      In Plan 02, _scan_line() handled the full line including leading whitespace skip.
      In Plan 03, the outer loop handles blank/comment skip and calls _handle_indentation.
      _scan_line() now only scans the CONTENT portion of the line (after indentation has been handled).
      Keep the per-character dispatch exactly as written in Plan 02.
      Remove the "skip leading whitespace" block from _scan_line() — the outer loop already
      advanced past the indent chars before calling _scan_line.
      At the END of _scan_line() (after processing the content chars), emit NEWLINE:
        source_line = raw_line.rstrip('\n')
        _emit_token(TokenType.NEWLINE, "", 0, source_line)

      Wait — NEWLINE must not be emitted for the last line if it has no trailing '\n' and the
      EOF drain will handle it. Actually: emit NEWLINE at the end of every _scan_line() call.
      The _drain_at_eof() method checks whether the last token is already NEWLINE before emitting
      a second one, so the last-line-no-trailing-newline case is handled there.

    CALL SEQUENCE in tokenize() per logical line:
      1. self._line = line_index + 1
      2. self._col = 0
      3. source_line = raw_line.rstrip('\n')
      4. if not raw_line.strip() or raw_line.lstrip().startswith('#'): continue
      5. width, indent_chars = self._measure_indent(raw_line)
      6. self._handle_indentation(width, indent_chars, source_line)
      7. self._col = width  ← advance col to start of content
      8. self._pos = (absolute position of first content char in self._source — needs tracking)

      POSITION TRACKING NOTE: The global self._pos cursor is tricky to maintain when the outer
      loop iterates over pre-split lines. Simplest fix: abandon the global pos cursor for the
      outer loop and instead compute the absolute position of the first content char:

      Best approach: track `self._pos` as the absolute offset in self._source. In the outer loop,
      after skipping/handling indentation, set self._pos to the offset of the first content char
      (which is line_start_pos + width). Compute line_start_pos by accumulating line lengths:
      track it as a running counter `line_start = 0` and `line_start += len(raw_line)` after each line.

      CLEANEST ALTERNATIVE: do not use self._pos for the outer loop at all. Pass the raw_line
      to _scan_line as a local string; _scan_line iterates over it with a local index `i` instead
      of the global self._pos. self._pos is then only used by _advance()/peek()/current() within
      a line. This isolates the global cursor to within-line scanning.

      FINAL DECISION (to minimize changes to Plan 02's working scanner):
      Keep _advance()/_current()/_peek() using the global self._pos into self._source.
      In the outer loop, before calling _scan_line (now renamed to process the content portion),
      set self._pos to the absolute offset of the first content char in the source.
      Track `abs_pos = 0` before the loop; at each line: content starts at abs_pos + width;
      set self._pos = abs_pos + width; after processing, abs_pos += len(raw_line).

      OR: Rename _scan_line to receive the raw_line and use a LOCAL index, entirely replacing
      the global cursor for within-line scanning. This is the cleanest and safest approach:

      _scan_line(self, raw_line: str, start_col: int) -> None:
        source_line = raw_line.rstrip('\n')
        i = start_col  ← start after the indent chars
        while i < len(raw_line) and raw_line[i] != '\n':
          ch = raw_line[i]
          ... (same dispatch as Plan 02 but using local i and advancing it)
        # emit NEWLINE at end
        _emit_token(TokenType.NEWLINE, "", 0, source_line)

      If using a local index, update self._col to match as tokens are emitted. This avoids
      the global cursor entirely for within-line scanning. Either approach is acceptable —
      the executor should choose the approach that minimizes divergence from Plan 02's working
      code while satisfying the structural test assertions.

    ERROR MESSAGE KEY PHRASES (must match what tests assert):
    - test_L4_mixed_tabs_spaces_error asserts "tabs and spaces" in ec.report()
      → error message must contain the phrase "tabs and spaces"
    - test_L4_staircase_dedent_error asserts "doesn't match" in ec.report()
      → error message must contain "doesn't match"
    - test_L4_over_indent_error asserts "too far" in ec.report()
      → error message must contain "too far"
    - test_L4_ragged_width_error asserts "same size" in ec.report()
      → error message must contain "same size"

    PITFALL GUARD — the "Looks Done But Isn't" checklist from PITFALLS.md:
    - Blank line inside block: test_L3_blank_line_no_tokens — must produce zero INDENT/DEDENT/NEWLINE
    - Deep comment: test_L3_deep_comment_no_indent_effect — 1 INDENT, 1 DEDENT total
    - EOF drain: test_L2_eof_drain_mid_block — 2 INDENT, 2 DEDENT (drains both open blocks)
    - Staircase dedent: test_L4_staircase_dedent_error — must ERROR, not silently produce wrong tokens
    - Mixed tab/space check: fires on leading whitespace characters, not on mid-line spaces

    SECURITY — non-termination invariant:
    _handle_indentation() does NOT loop. It is called once per line. The pop loop (`while
    self._indent_stack[-1] > width`) terminates because the stack shrinks on each iteration.
    No path in _handle_indentation can cause an infinite loop as long as the stack is initialized
    with [0] and width is a non-negative integer (both invariants maintained by construction).

    COLLECT-ALL (D-04): After any error in _handle_indentation(), scanning continues. The method
    returns normally after logging the error — it does not raise or return early from tokenize().
  </behavior>

  <action>
    Modify src/atena/lexer.py. Read the Plan 02 file first to understand the exact working scanner
    implementation. Then:
    1. Add _measure_indent(self, raw_line: str) -> tuple[int, str].
    2. Add _handle_indentation(self, width: int, indent_chars: str, source_line: str) -> None
       implementing standard stack + uniform-step + tab/space mixing checks. Use the exact key
       phrases required by the tests: "tabs and spaces", "doesn't match", "too far", "same size".
    3. Add _drain_at_eof(self) -> None implementing the EOF NEWLINE + DEDENT drain + final EOF token.
    4. Restructure tokenize() with the line-indexed outer loop: set self._line per loop iteration,
       skip blank/comment lines, call _measure_indent, call _handle_indentation, then call the
       content scanner, then _drain_at_eof(). Do NOT restructure _scan_line's character-level
       dispatch — keep all Plan 02 token-type logic exactly. Adjust self._pos and self._col
       tracking as needed so the within-line scanner works correctly.
    4a. CRITICAL refactor of the Plan 02 _advance() contract: Plan 02's _advance() increments
        self._line when it consumes '\n'. Under this line-indexed outer loop, line tracking is
        owned by the loop (self._line = line_index + 1 at the top of each iteration). You MUST
        remove the `self._line += 1` side-effect from _advance() — _advance() now updates ONLY
        self._pos and self._col. Leaving the increment in place double-counts lines and corrupts
        every token's line stamp. Verify no other `self._line += 1` remains in _advance() after editing.
    5. At the end of each content-bearing line, emit NEWLINE before moving to the next line.
    Do NOT include fenced code blocks in this action. Reference the behavior block for exact
    algorithm descriptions. Do NOT emit Python jargon in any error message string.
  </action>

  <verify>
    <automated>python3 -m pytest tests/test_lexer.py -q --tb=short 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - python3 -m pytest tests/test_lexer.py -q exits 0 with 28 passed, 0 failed.
    - python3 -m pytest tests/ -q exits 0 with 86 passed (58 Phase 0 + 28 lexer), 0 failed.
    - python3 -m pytest tests/test_lexer.py::test_L2_indent_dedent_balanced -xvs exits 0.
    - python3 -m pytest tests/test_lexer.py::test_L2_eof_drain_mid_block -xvs exits 0.
    - python3 -m pytest tests/test_lexer.py::test_L3_deep_comment_no_indent_effect -xvs exits 0.
    - python3 -m pytest tests/test_lexer.py::test_L4_staircase_dedent_error -xvs exits 0.
    - python3 -m pytest tests/test_lexer.py::test_L4_mixed_tabs_spaces_error -xvs exits 0.
    - python3 -m pytest tests/test_lexer.py::test_L4_over_indent_error -xvs exits 0.
    - python3 -m pytest tests/test_lexer.py::test_L4_ragged_width_error -xvs exits 0.
    - grep -v "^#" src/atena/lexer.py | grep -c "def _handle_indentation" returns 1.
    - grep -v "^#" src/atena/lexer.py | grep -c "def _drain_at_eof" returns 1.
    - grep -v "^#" src/atena/lexer.py | grep -c "tabs and spaces" returns at least 1 (error message present).
    - grep -v "^#" src/atena/lexer.py | grep -c "doesn.t match" returns at least 1 (error message present).
  </acceptance_criteria>

  <done>
    All 28 lexer tests GREEN. All 58 Phase 0 tests GREEN. Total suite: 86 passed, 0 failed.
    Phase gate passed — Phase 1 is 100% green across all test layers (golden token snapshots + error-path message/count/order assertions). src/atena/lexer.py delivers LEX-01 through LEX-08 as specified.
    Commit message: feat(01-03): complete lexer — INDENT/DEDENT/uniform-step/EOF drain (all 28 tests GREEN)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Learner source text → _handle_indentation | Width values come from _measure_indent which counts chars in the raw source line — untrusted user input. Must not cause exceptions. |
| Learner source text → _drain_at_eof | EOF processing uses the indent stack state; a deeply-nested file emits one DEDENT per stack entry. Stack depth is bounded by the nesting level in the source (iterative loop, not recursive — Python stack is safe). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Denial of Service | _handle_indentation (pop loop) | mitigate | Pop loop terminates because stack shrinks on each iteration AND stack always starts with [0] so the loop exits when stack[-1] <= width. No infinite loop is possible. Verified by staircase-dedent test which would hang if the loop did not terminate. |
| T-01-03-02 | Denial of Service | Unbounded indent stack (deeply nested source) | accept | Teaching context (v1.0 programs); nesting depth bounded by source size. The pop loop is iterative (no Python recursion), so no Python stack overflow. Accepted for v1.0. |
| T-01-03-03 | Tampering (no traceback) | Indentation error reporting | mitigate | All indentation errors call ErrorCollector.add(); scanning continues after each one (D-04). No exception raised. test_L4_* tests confirm no Python exception escapes on adversarial indentation. |
| T-01-03-04 | Tampering | Tab/space detection (mixed-char) | mitigate | Detection checks LEADING characters only (not mid-line spaces). The check compares each char in indent_chars against the pinned self._indent_char. A learner file mixing tabs/spaces receives a specific plain-English error (test_L4_mixed_tabs_spaces_error must be GREEN). |
| T-01-03-SC | Tampering | No package installs | accept | Zero new packages. stdlib + sibling modules only. |
</threat_model>

<verification>
After Task 1 completes:
- python3 -m pytest tests/test_lexer.py -q shows 28 passed, 0 failed (all GREEN).
- python3 -m pytest tests/ -q shows 86 passed, 0 failed (Phase 0 + Phase 1 green).
- python3 -m pytest tests/test_lexer.py::test_L2_indent_dedent_balanced -xvs: PASSED.
- python3 -m pytest tests/test_lexer.py::test_L3_blank_line_no_tokens -xvs: PASSED.
- python3 -m pytest tests/test_lexer.py::test_L4_staircase_dedent_error -xvs: PASSED.
- python3 -m pytest tests/test_lexer.py::test_L4_over_indent_error -xvs: PASSED.
</verification>

<success_criteria>
Phase 1 is complete: 28/28 lexer tests GREEN, 58/58 Phase 0 tests GREEN (86 total). The Lexer delivers:
- All 19 TokenType values produced for appropriate source (LEX-01)
- Balanced INDENT/DEDENT with EOF drain (LEX-02)
- Blank/comment-line skipping with zero stack side-effects (LEX-03)
- Uniform-step indentation enforcement with plain-English errors for mixed tabs/spaces, staircase-dedent, over-indent, and ragged-width (LEX-04)
- Maximal-munch `=`/`==` and all comparison/arithmetic operators (LEX-05)
- All 19 keywords via KEYWORDS dict (LEX-06)
- Double-quoted strings + integers stamped with line/col/source_line (LEX-07)
- Plain-English errors for unterminated string, four off-ramps, unexpected char (LEX-08)
The output is a flat, INDENT/DEDENT-balanced list[Token] terminated by EOF — ready for Phase 2 Parser.
</success_criteria>

<output>
Create .planning/phases/01-lexer/01-03-SUMMARY.md when done.
</output>
